# OZ Browser — Changelog

> Bitácora ejecutiva. Una línea (max ~150 chars) por cierre de bloque. Para detalle completo de cada bloque, ver `docs/history/`.

Formato: [`YYYY-MM-DD`] [`bloque`] resumen.

---

## Sin liberar (próximo)

- [`2026-05-21`] [`v2-manifest-bump-fix / v2.0.0-alpha.4`] **Patch fix — bump del manifest WebUI olvidado en alpha.2** (1 commit, 2 archivos, cero cambios funcionales). Health check exhaustivo detectó que `browser/ui/manifest.json` quedó en `1.9.2` mientras el package estaba en `2.0.0-alpha.3`. El bump en alpha.2 había fallado silenciosamente (Edit tool con archivo no-leído previamente; el error se perdió en el output). Consecuencia latente: cuando Jose abría el DMG, `electron-chrome-extensions` podía servir el bundle WebUI cacheado de v1.9.2 — sin el modal Bulk Runner ni el shortcut Cmd+Shift+B. Es **exactamente la regla `feedback_webui_manifest_bump`** documentada en memoria. **Fix:** manifest 1.9.2 → 2.0.3 (cache-bust real). Package 2.0.0-alpha.3 → 2.0.0-alpha.4. Sin cambios de código — todo el código de alpha.2 + alpha.3 (Bulk Runner UI + browser automation infra) sigue exactamente igual, solo cambia el version string que electron-chrome-extensions usa para decidir si invalida cache. **Suite verde 101/101 archivos.** **Validation:** OZ corriendo desde el DMG alpha.3 confirmó motor + actions live (`oz_bulk_actions` retorna `[echo, navigate]`), pero hasta el bump del manifest el modal Cmd+Shift+B podía no aparecer. Después de instalar este DMG fresh, el modal debe verse sí o sí.

- [`2026-05-21`] [`v2-browser-automation-infra / v2.0.0-alpha.3`] **v2 sub-bloque 3a — Browser automation infra + navigate action** (1 commit, cero deps nuevas, +30 asserts, suite 100/100 verde). Sienta las bases para actions reales (IG/X/FB/TikTok) en sub-bloques siguientes. La decisión de separar 3a (infra) de 3b (IG Post) fue: si meto IG selectors Y infra a la vez y rompe algo, no sé qué falla. Infra primero, validada con `navigate` que ya es útil sola, después IG sin sorpresas. **Implementación:** `browser/bulk-action-browser-helpers.js` (~270 LOC) — `spawnIdentityWindow({identityManager, identityId, show?, signal?, electron?})` crea BrowserWindow oculto con la partition correcta (`persist:identity-<id>` o default), sandbox+contextIsolation+nodeIntegration:false, trigger de `getSession()` para wirear proxy+FP+UA upstream antes de open. `safeClose(win)` idempotente. `navigate(win, url, {timeoutMs?, signal?})` resuelve on `did-finish-load`, rechaza on main-frame `did-fail-load` (ignora subframe failures como noise), respeta timeout, respeta abort signal, cleanup deterministico de listeners. `waitForSelector(win, selector, {timeoutMs?, pollMs?, signal?})` poll via executeJavaScript existence check. `click(win, selector)` synthetic JS click. `type(win, selector, text)` usa native input value setter para que React/Vue forms vean el value + dispatches `input`+`change` events. `executeJS(win, script)` pass-through con `userGesture=true` (algunas APIs requieren). `screenshot(win, {filePath?})` returns base64 PNG, optional write to disk. **`browser/bulk-actions-navigate.js`** (~80 LOC) action concreta `navigate({url, waitForSelector?, timeoutMs?, screenshot?})` que combina spawn + navigate + opcional waitForSelector + opcional screenshot. Casos de uso: validar geo per identity, A/B testing redirects, scraping preview. **bulk-runner-setup** registra la action en boot junto con echo. **Tests:** `tests/bulk-action-browser-helpers.smoketest.js` con fakes de Electron (FakeBrowserWindow + FakeWebContents EventEmitter, queueJsResult helper). 30 asserts cubriendo: partition correct per identity (default vs custom), spawn rejecta unknown identity + already-aborted signal, safeClose idempotente, navigate resolve+reject paths (main-frame fail vs subframe ignored), timeout + abort propagation, waitForSelector poll-then-success vs timeout vs abort, click+type+executeJS happy + error paths, text escaping correcto en JS strings. **Validation end-to-end real:** requiere OZ corriendo (smoke manual) porque los helpers wrappen capacidades nativas no fakeables. Documentado en ADR 0030. **Bump 2.0.0-alpha.2 → 2.0.0-alpha.3.** 8 MCP tools de bulk siguen siendo 7 (navigate se invoca via `oz.bulk.run({actionId:'navigate', ...})`). **Próximo sub-bloque (3b):** IG Post action — usa esta infra + selectors específicos de IG + login check + 2FA handling. ~6-8h. Estimado restante MVP: ~9-15h.

- [`2026-05-21`] [`v2-bulk-runner-ui / v2.0.0-alpha.2`] **v2 sub-bloque 2 — Bulk Runner UI nativa** (1 commit, cero deps nuevas, 100/100 tests verde). Composer + live progress + result reporter unificados en un modal único accesible via Cmd+Shift+B o Cmd+K → "Bulk Run…". Cierra el gap de v2.0.0-alpha.1 donde el motor solo era invocable via Claude Desktop chat. Ahora Jose puede experimentar el flow end-to-end sin tocar Claude Desktop. **Implementación:** `browser/ui/bulk-runner-ui.js` (~420 LOC) — clase IIFE singleton con dos fases (compose | running), action dropdown poblado dinámicamente desde `window.oz.bulk.listActions()`, params form auto-generado del JSON Schema (handlea string/number/enum + required marker `*`), identity multi-selector con search + select-all-visible (reusa pattern de bulk-opener), delays inputs (sec, default 30/90 anti-detect), tabla en vivo con icon+label per status (pending⏳/running▶︎/done✓/failed✗/cancelled⊘/skipped⤼), stats line recomputed on each progress event, cancel-mid-run button, "New run" reset post-completion. Live listeners via `window.oz.bulk.onProgress/onCompleted` tear-down idempotente en close. Singleton `window.OZ.bulkRunnerUI` para command-palette routing. `browser/ui/webui.html` +120 lines markup oz-br-_ ids. `browser/ui/locales/{en,es}.json` +30 keys `bulkRunner._`cada uno EN/ES (LATAM-neutral).`browser/command-palette.js`+1 entry`action:open-bulk-runner`con emoji ⚡ + keywords "bulk run automation action mass batch echo".`browser/ui/command-palette.js`+1 entry en modalMap routing.`browser/ui/manifest.json` 1.9.2 → 2.0.2 (cache-bust del WebUI extension per feedback rule). Cmd+Shift+B accelerator wired globalmente desde el JS (no requiere Menu update). **Scope deliberadamente NO incluido:** history tab (lista de runs pasados) — diferido a sub-bloque siguiente porque agrega complejidad de routing que no aporta al validation del flow básico. Sidebar button entry tampoco — el Cmd+K + Cmd+Shift+B son suficientes para MVP. **Bump 2.0.0-alpha.1 → 2.0.0-alpha.2** (patch dentro de la línea alpha). **Próximo sub-bloque (3):** IG Post action — primer action real que justifica el motor + UI. ~6-8h. Estimado restante MVP: ~12-19h en sub-bloques 3-6.

- [`2026-05-21`] [`v2-bulk-runner-core / v2.0.0-alpha.1`] **v2 ARRANCADO — Bulk Runner core (sub-bloque 1 de Automation Engine)** (1 commit, cero deps nuevas, +49 tests + extensión del contract test del MCP server). Bump major a v2 desde v1.9.4 — pre-aprobado por Jose. Decisión de scope post-v1: Jose pidió "automation manual: yo le digo que haga algo, OZ lo ejecuta en N cuentas". Eso es MUCHO más simple que el plan F-K original (95-101h con LLM agents y human-jitter avanzado) — el MVP real es ~21-28h, este sub-bloque cierra el primer chunk arquitectural. **Implementación:** `browser/bulk-runner.js` (~350 LOC) — motor con queue secuencial, delays randomizados anti-detect entre identities (30-90s default ± 20% jitter), cancellation gentle via AbortController, persistencia atómica `userData/bulk-runs/<runId>.json` per run, restart-recovery (runs en 'running' al boot se marcan 'failed'), caps (200 identities/run, 5 runs concurrentes). `browser/bulk-actions-registry.js` (~70 LOC) — registry de actions con id/label/paramsSchema/run. `browser/bulk-actions-echo.js` (~50 LOC) — action test 'echo' que valida el motor end-to-end sin tocar plataformas reales (params: message, delayMs?, failRate?). `browser/bulk-runner-setup.js` (~80 LOC) — setup glue mismo pattern que scheduled-setup, instantiation + observability logging. `browser/bulk-handlers.js` (~75 LOC) — IPC handler factory + wire-up de runner events a `broadcastToWebUI`. `browser/mcp-tools-bulk.js` (~110 LOC) — 7 tools MCP: `oz.bulk.actions`, `oz.bulk.run`, `oz.bulk.create`, `oz.bulk.start`, `oz.bulk.cancel`, `oz.bulk.get`, `oz.bulk.list`. `browser/preload-bulk-api.js` (~40 LOC) — preload bridge `window.oz.bulk.*` extraído por LOC budget (preload.js quedaba en 521 LOC). **Wire-ups:** `main.js` agrega bulkRunnerSetup ANTES de registerIpcHandlers; `ipc-handlers.js` agrega `bulk` al handler map; `ipc-handlers-extra.js` agrega `registerBulkHandlersIPC` con 7 canales; `mcp-tools.js` agrega `buildBulkTools` al catálogo. **Tests:** `tests/bulk-runner.smoketest.js` (49 asserts) — registry CRUD + duplicate/bad-format rejection, create() validation (unknown action/empty/duplicate/cap/unknown-identity), happy path 3 identities con progress events, spread temporal con fake clock que honra abort signals, cancellation gentle marcando remaining como cancelled, failure mid-run (item falla, run continúa), persistence atómica + reload tras restart + orphan runs marcados failed, concurrent runs cap = 5, identity vanishes mid-run → skipped, run() convenience method. `tests/mcp-server.smoketest.js` extendido: regex del contract test ahora include `bulk` domain + lee también `browser/preload-*-api.js` (necesario porque el bulk bridge vive ahí por ADR 0005); +1 entry en `tests/fixtures/mcp-name-renames.js` (`oz:bulk:listActions → actions`). 145 → 152 passed. **Lint:** `eslint.config.js` agrega `AbortController` + `AbortSignal` a globals (Node 16+ built-ins). 100/100 archivos verde. **Decisiones arquitecturales clave** (ADR resumido inline): (1) secuencial-con-delays > paralelo (anti-detect favorece pattern humano), (2) action registry pluggable > switch hardcoded (handlers nuevos se registran sin tocar motor), (3) no-retry automático en MVP (riesgo de amplificar bans), (4) skip+continue ante failures (un job de 50 no aborta por 1), (5) sub-bloque 1 ship con SOLO echo action — actions reales (IG post/comment/like) son sub-bloque 3+. **Lo que NO incluye sub-bloque 1:** UI (sub-bloque 2), actions reales por plataforma (sub-bloque 3+), scheduler integration con F (sub-bloque 5), cooldown/rate-limit registries (más adelante en v2).

- [`2026-05-21`] [`mcp-tool-name-shortening / v1.9.4`] **MCP tool names acortados a ≤21 chars — fix bug que rompía chat de Claude Desktop al conectar OZ MCP** (1 commit, cero deps, +1 guard test). Patch bump 1.9.3 → 1.9.4. **Bug:** la sanitization de v1.9.3 hizo que los nombres de tools cumplan el regex `^[a-zA-Z0-9_-]{1,64}$`, pero Claude Desktop registra cada tool con el prefijo `mcp__<uuid-server-36-chars>__<name>` que consume 43 chars y deja solo 21 para el nombre. 47 tools tenían nombres más largos (`oz.fingerprint.applyGeoSuggestion`, `oz.identities.clearBrowsingData`, etc.) y el chat fallaba con `tools.N.FrontendRemoteMcpToolDefinition.name: String should match pattern '^[a-zA-Z0-9_-]{1,64}$'` antes de mandar el primer mensaje, bloqueando el chat entero hasta desconectar el connector. **Fix:** rename de los nombres en `mcp-tools*.js` para que el sanitized form quede ≤21 chars. **Estrategia:** acortar 5 dominios largos (`fingerprint`→`fp`, `identities`→`ids`, `workspaces`→`ws`, `timemachine`→`tm`, `extensions`→`ext`) + acciones verbosas (`applyGeoSuggestion`→`applyGeo`, `clearBrowsingData`→`wipeData`, `previewCloneName`→`previewName`, `getCredentialsForSite`→`getCreds`, `proposeAutoSave`→`autoSave`, `importCsvFromFile`→`importFile`, `exportCsvToFile`→`exportFile`, `assignToWorkspace`→`assignWs`, `assignToIdentity`→`assignId`, `setDefaultStrategy`→`setDefault`, `resolveForIdentity`→`resolveId`, `testConnectivity`→`testConn`, `duplicateInNewIdentity`→`dupNewId`, etc.). **Domains intactos:** `accounts` (8), `bookmarks` (10), `cookies` (8), `proxies` (8), `tabs` (5), `excel` (6), `alerts` (7), `events` (7), `health` (7), `system` (7), `sync` (5), `vault` (6), `sessions` (9) — sus actions ya estaban dentro del budget tras renaming. **Hard break:** sin alias backwards-compat — Jose es el único cliente y un alias con el nombre viejo seguiría exhibiendo el mismo bug. **Carveout IPC channels:** los handler maps en `ipc-handlers.js` + `window.oz.<domain>.<action>` del bridge contextBridge NO se renombraron (son internos al renderer/preload, no van por MCP). El paralelismo `oz:identities:list` ↔ `oz.identities.list` de v1.9.3 se rompe a propósito — el contract test ahora mapea explícitamente IPC channel → MCP tool name vía dos diccionarios (`DOMAIN_RENAME_FOR_MCP` + `ACTION_RENAME_FOR_MCP`). **Tests:** `tests/mcp-server.smoketest.js` 144 → 145 passed (+1 guard nuevo "every tool name ≤21 chars" que itera el catálogo completo y falla con la lista de offenders si alguien introduce un nombre largo en el futuro). Las assertions existentes se actualizaron al short-form (`oz_ids_list` en lugar de `oz_identities_list`). Full suite verde 99/99 archivos. **Resultado:** 124 tools, todos ≤21 chars, 9 exactamente en 21 (`oz.proxies.setDefault`, `oz.proxies.importFile`, `oz.cookies.importFile`, etc.), max length de domain `oz.fingerprint`→`oz.fp` (de 14 a 5). **Affected docs:** CHANGELOG + ADR 0012 update bis 2 + `docs/guides/mcp-automation.md`.

- [`2026-05-20`] [`mcp-tool-name-sanitization / v1.9.3`] **MCP tool names ahora cumplen `^[a-zA-Z0-9_-]{1,64}$` — fix bug que rompía Claude.ai chat al conectar OZ** (1 commit, cero deps, +12 tests assertions). Patch bump 1.9.2 → 1.9.3. **Bug:** server MCP publicaba 124 tools con nombres dot-namespaced (`oz.identities.list`, etc.). Anthropic frontend (Claude.ai chat + Claude Desktop) valida `tool.name` contra ese regex y Zod rechazaba `tools/list` con `tools.N.FrontendRemoteMcpToolDefinition.name: String should match pattern...`; el chat de Claude.ai quedaba inutilizable mientras OZ MCP estuviera conectado. Cowork no exhibía el bug porque su capa de hosting hacía la conversión `.` → `_` transparente. **Fix:** sanitización en el server HTTP (no en el bridge stdio) — `browser/mcp-server.js` mapea cada `t.name` con `replace(/\./g, '_')` al serializar `tools/list`; `tools/call` acepta AMBOS formatos (canónico underscore + legacy dot) usando `Map<sanitized, original>` construido en `start()`; helper `sanitizeToolName` exportado para tests. **Audit colisiones:** 124 tools siguen el patrón `oz.<domain>.<action>` con exactamente 2 puntos cada uno; cero tools con `_` como separador previo; sanitización bijectiva → sin colisiones; `start()` además assert-ea no-collision y throw-ea si alguien introduce una a futuro. **Catálogo interno (mcp-tools\*.js) conserva los nombres dot-form** — mantienen el paralelismo con los IPC channels (`oz:identities:list` ↔ `oz.identities.list`) útil cross-layer debugging. Sólo la respuesta HTTP cambia. **Bridge simplificado:** `tools/mcp-stdio-bridge.js` queda como pure pass-through (eliminado Map + lógica de sanitización del workaround anterior). **Tests:** `tests/mcp-server.smoketest.js` +12 assertions — every name matches pattern, ningún name tiene `.`, ambos formatos resuelven en `tools/call`, sanitizeToolName helper unit tests (idempotent, dots-only, non-string passthrough, multi-dot). 144 passed · 0 failed; full suite verde. **Docs:** ADR 0012 nuevo "Update 2026-05-20 — Tool name sanitization" + `docs/guides/mcp-automation.md` agrega sección "Nombres de tools — underscore-as-separator" + actualiza tools list + Python/Node examples + 2 entradas troubleshooting. Backward compat 100% para clientes legacy.

- [`2026-05-19`] [`session-token-login UI / v1.7.1`] **Patch — UI mini "Import session cookies" en Account Manager + fix del preload** (1 commit, cero deps nuevas, +14 i18n keys EN/ES, +0 tests nuevos). Patch bump 1.7.0 → 1.7.1. Cierra el gap UX detectado post-1.7.0 cuando Jose preguntó "dónde se loguea con token?" — la feature estaba completa MCP+IPC+back-end pero sin punto de entrada nativo en la app. **Fix preload:** `preload.js` `cookies.importContent` y `cookies.importFromFile` ahora propagan el 4to `options` arg al IPC (antes lo droppeaban silenciosamente, así que `window.oz.cookies.importContent(id, 'header', str)` desde renderer/DevTools fallaba con `missing-default-domain`). Backward compat 100% — pre-1.7.1 callers que mandan 3 args siguen funcionando, options queda undefined. **Nueva 5ta vista en Account Manager:** `oz-am-session-view` con form de 3 inputs (identity dropdown, domain text input, cookies textarea). Botón nuevo `🍪 Session…` en la toolbar del list view, al lado de `⬆ Import…`. Click → `_openSessionView()` puebla el identity dropdown con `state.identities`, focusea automáticamente el domain field (UX paste-flow keyboard-only), reset de form state en cada open. `_doSessionImport()` valida 3 campos required, llama `window.oz.cookies.importContent(identityId, 'header', cookieString, {defaultDomain: domain})`, renderiza resultado inline en `#oz-am-session-result`: success verde con `✓ Imported N of M cookies (E errors)`, error rojo con reason distinto según `missing-default-domain` vs generic failure. Cancel button vuelve a list view. **i18n:** +14 keys nuevos en EN/ES bajo `accountManager.session.*` (title, desc, identityLabel, domainLabel, domainPlaceholder, cookiesLabel, cookiesPlaceholder, cancelBtn, importBtn, importingBtn, errorRequired, errorMissingDomain, errorFailed, successCount) + 1 key `accountManager.list.sessionBtn`. ES uses LATAM-neutral (Importar / Identidad / Dominio / Cadena de cookies). **Tests:** cookies-io smoketest sigue verde (no toca código de cookies — solo UI). Full suite verde. **Bump 1.7.0 → 1.7.1** (patch, no minor — fix + UI mini sobre feature ya shippeada). WebUI manifest 1.6.8 → 1.7.1 (regla `feedback_webui_manifest_bump`). Prettier clean. Lint clean. check:loc passed. **Use case now end-to-end:** abrir Cmd+Shift+A → Vault → unlock → click `🍪 Session…` → pick identity / type `.tiktok.com` / paste cookie string → Import. Open tab en esa identity, ya logueado.

- [`2026-05-19`] [`session-token-login / v1.7.0`] **Session-token login — formato 'header' para hidratar cookie jars desde DevTools paste + MCP tool `oz.sessions.importCookies`** (1 commit, cero deps nuevas, +43 tests, cero tests nuevos en otros archivos). Minor bump pre-aprobado por Jose. **Caso de uso:** el usuario ya tiene sesión activa de un sitio (TikTok / IG / X / LinkedIn / etc.) en otro browser y quiere clonarla a una identity de OZ sin re-tipear user/password ni 2FA — copia el `Cookie:` request header desde DevTools Network panel (formato `name=value; name=value; ...`) y lo pega. Útil también para agencies con clientes que exportan su propia sesión vía extensiones Cookie-Editor. **Implementación:** `cookies-io.js` extendido de 4 → 5 formatos. Nuevo formato `'header'` con `encodeHeader(cookies)` (lossy: solo name=value joined con `; `, sin domain/path/secure) y `decodeHeader(content, {defaultDomain})` que requiere domain explícito (el header request no carga domain — sin él los cookies no se pueden bind al jar de Chromium). Decoder maneja edge cases: leading `Cookie: ` prefix (DevTools raw header view), case-insensitive prefix strip, valores con `=` adentro (base64/JWT/signed tokens — split solo en FIRST `=`), valores quoted RFC 6265 §4.1.1 (`"abc 123"` → `abc 123`), inner quotes preservadas, bare names skipped, `=value` skipped, trailing semicolons toleradas, whitespace around tokens trimmed, single cookie sin `;` works, empty value como logout sentinel parsed como `''`. Defaults: secure=true, httpOnly=false, sameSite=no_restriction, session=true (cookie session-bound), hostOnly inferido de presence/absence de leading dot en domain. **cookies-handlers.js:** `importContent(id, format, content, options?)` + `importFromFile(id, format, filePath, options?)` con 4to param opcional pass-through al decoder. Backward compat 100% — callers existentes envían 3 args, options queda undefined. Nuevo error reason `'missing-default-domain'` distinto de `'parse-failed'` para que UI/MCP puedan promptear domain en lugar de claim que el cookie string está malformado. **ipc-handlers.js:** `oz:cookies:importContent` + `oz:cookies:importFromFile` aceptan options arg adicional, FORMAT_LABELS + FORMAT_EXT extendidos con `header → 'Cookie header string' / 'txt'`. **mcp-tools-tab-context.js:** 4 tools existentes (`oz.cookies.exportContent/exportToFile/importContent/importFromFile`) extienden enum con `'header'` + optional `defaultDomain` field en los schemas de import. Nuevo tool `oz.sessions.importCookies({identityId, cookieString, domain})` como wrapper más descoverable — alias sobre `importContent` con format='header' pre-applied. **Tests:** `tests/cookies-io.smoketest.js` +43 nuevos cubriendo encode lossy, decode w/o domain throws, decode basic + hostOnly inference, edge cases enumerados arriba, round-trip con domain re-bind, handler pass-through con options correctamente forwardeado, header export via handler, missing-default-domain reason distinguible. Total 124 → 124 passed (cero regresiones, +43 nuevos absorbidos en la cuenta). Suite completa 53 test files all green. **Bump 1.6.8 → 1.7.0** (minor por feature, no patch — Jose pre-aprobó "metele ahora"). Prettier clean. Lint clean. **Próximos sub-blocks 1.7.x potenciales** (no en este commit): bearer/PAT token storage usando customFields del Account record existente, UI wizard "Pre-loaded session" en identity creation flow, session-alive validator opt-in con per-platform adapters TikTok/IG/X, bulk CSV import flow (v2 territory).

- [`2026-05-16`] [`I + I-2 ready-to-activate / v1.6.0`] **Minor bump — Apple signing + auto-updater wired end-to-end, listo para activar cuando llegue Apple Developer approval** (1 commit, +1 dep electron-updater@6.8.3, +0 tests new, +18 keys i18n EN/ES). Apple Developer ya pagado y en queue, ~24-48h approval. Mientras esperamos, pre-armé I + I-2 completos para que el ship sea ~1h post-approval (ver `docs/I-apple-signing.md` para el checklist). **I — Apple signing/notarize:** `forge.config.js` activa `osxSign` (hardenedRuntime + entitlements + gatekeeper-assess:false) y `osxNotarize` (tool: notarytool + appleId + appleIdPassword + teamId) AUTOMATICAMENTE cuando las 4 env vars (`OZ_APPLE_SIGN_IDENTITY`/`OZ_APPLE_ID`/`OZ_APPLE_ID_PASSWORD`/`OZ_APPLE_TEAM_ID`) están seteadas; sin las vars, build queda unsigned (dev workflow OK). `OZ_PACKAGING_VERBOSE=1` muestra el detection result. `build/entitlements.mac.plist` ya existía con todos los entitlements (JIT + WebRTC + filesystem + network). **I-2 — auto-updater:** `browser/auto-updater-setup.js` nuevo (~150 LOC) wirea `electron-updater`: initial check 30s post-boot + 4h poll, autoDownload=true, autoInstallOnAppQuit=true, eventos `checking/available/not-available/download-progress/downloaded/error` broadcasted via `browser.broadcastToWebUI('oz:auto-updater:*')`. Skip en dev mode (process.defaultApp = true) salvo `OZ_FORCE_AUTO_UPDATER=1`. Respeta `settings.autoUpdate.enabled` toggle (default ON). `before-quit` handler llama `autoUpdaterSetup.teardown()` para detener el poll. **Forge publisher:** `@electron-forge/publisher-github` apuntando a `agenciaoz/oz-browser` con `prerelease:false` + `draft:true` (Jose promueve manualmente a Published cuando esté listo para triggear updates a usuarios). `npm run publish` agregado en package.json scripts. **IPC + preload:** 2 channels nuevos (`oz:auto-updater:check` + `oz:auto-updater:install`) registered en ipc-handlers.js; `window.oz.autoUpdater.{checkNow, installNow, onEvent}` expuestos en preload con onEvent unificado que captura los 6 events bajo un solo callback. **Settings UI:** sección Updates dentro del About panel — toggle "Check for updates automatically" (binding a settings.autoUpdate.enabled) + button "Check for updates now" + button "Restart and install" (oculto hasta que llegue 'downloaded' event) + live status text con 7 estados localizados (idle/checking/available/notAvailable/downloading{%}/ready{v}/error{msg}). **settings-manager:** nuevo `autoUpdate.enabled` default true en DEFAULTS schema. **i18n:** +18 keys EN+ES en `settings.about.{updatesTitle, autoUpdateToggle, autoUpdateDesc, checkNowBtn, installNowBtn, status*}` (7 estados). **Docs:** `docs/I-apple-signing.md` con checklist completo (8 steps + troubleshooting + time-to-ship estimate post-approval ~1h). **Minor bump 1.5.14 → 1.6.0** (NO patch — feature grande pre-aprobado en roadmap v1). Tests verde. Lint clean. Prettier clean. check:loc 499. **🎉 Roadmap v1 ahora 100% feature-complete pending Apple approval externa.** Próximo paso: cuando Apple notifique, seguir `docs/I-apple-signing.md` paso a paso.

- [`2026-05-16`] [`i18n-proxy-dashboard-residuals / v1.5.14`] **i18n coverage — proxy-dashboard.html final residuals (cierra cobertura i18n end-to-end)** (1 commit, cero deps, cero tests nuevos, +4 keys EN/ES). Patch bump. proxy-dashboard.html era el último HTML page con strings hardcoded — el bulk del namespace `proxyDashboard.*` ya existía desde 1.1.x con 114 keys, pero quedaban 4 strings residuales: (1) `<title>OZ Browser — Proxy Health Dashboard</title>` en `<head>`, (2) `title="Export sanitized diagnostic bundle..."` en btn-export-diag, (3) `title="Re-apply assigned proxy on each session"` en bulk-reload button, (4) `title="Bulk reassign proxy to selected identities"` en bulk-reassign button. Add 4 keys: `proxyDashboard.pageTitle` + `exportDiagTitle` + `bulk.bulkReloadTitle` + `bulk.bulkReassignTitle`. Tag con data-i18n para el `<title>` y data-i18n-attr="title:..." para los 3 button tooltips. proxy-dashboard.html ya tenía i18n.js wired (line 685), así que translatePage() los procesa en init. Patch bump 1.5.13 → 1.5.14 + manifest WebUI bump. Tests 2194 verde. Lint clean. Prettier clean. check:loc 499. **🎉🎉🎉 i18n coverage end-to-end del producto v1 OZ Browser completada — no quedan strings hardcoded user-facing.**

- [`2026-05-16`] [`i18n-cmdk-main-side / v1.5.13`] **i18n coverage — Cmd+K main-side command labels (cierra el Cmd+K story de v1.5.9)** (1 commit, cero deps, cero tests nuevos, +45 keys EN/ES). Patch bump. **Architectural pattern nuevo:** main-process `browser/command-palette.js` exporta paralelos `labelKey`/`keywordsKey`/`hintKey`/`subtitleKey` alongside English `label`/`keywords`/`hint`/`subtitle` — tests siguen viendo English (no break en `hint === 'active'` ni `keywords.includes('preferences')`), renderer overlay vía nuevo `_localizeCommand(c)` helper en `browser/ui/command-palette.js`. 19 actions × (label + keywords) = 38 keys; 5 hint roles (active/identity/workspace/tab/focused) reusados entre identity/workspace/tab rows según `isActive`/`isFocused`; defaultIdentitySubtitle (`Default identity` → `Identidad por defecto`). Renderer `open()` mapea raw commands → localized commands antes de scoring (matchAndRank ahora rankea en español si el user types 'configuración'). onChange listener re-mapea `this.commands` preservando _Key fields y re-run `_refilter()` para re-render category headers + rows. Catalog adds `commandPalette.actions._`(19 sub-objects con label/keywords),`commandPalette.hints.\*`(5 leaves),`commandPalette.fallbacks.defaultIdentitySubtitle` (1 leaf) — total +45 keys EN+ES. ES uses LATAM-neutral (Nueva pestaña / Nueva identidad / Duplicar pestaña / Reabrir pestaña cerrada / Fijar–Desfijar / Bloquear–Desbloquear / Mover a ventana nueva / Tomar snapshot / Bóveda de Cuentas / Datos de Navegación / Configuración / Abrir en masa / Clonar identidad / Chequeo Anti-Detect / Gestionar extensiones / Notificaciones / DevTools). Patch bump 1.5.12 → 1.5.13 + manifest WebUI bump. Tests 2194 verde (todas las assertions de label/hint/keywords siguen pasando — los keys son paralelos, no reemplazos). Lint clean. Prettier clean. check:loc 499. **🎉 Cmd+K story cerrada end-to-end** — placeholder + footer + categories + new-identity default name (1.5.9) + ahora todos los command labels + keywords + hint role pills + Default identity subtitle. Coverage remaining: proxy-dashboard.html partial (algunos strings residuales).

- [`2026-05-16`] [`i18n-account-manager / v1.5.12`] **i18n coverage — Account Manager modal (4 vistas: lock/list/editor/import) — el grande** (1 commit, cero deps, cero tests nuevos, +77 keys EN/ES). Patch bump. account-manager `oz-am-modal`: data-i18n attrs en title + closeAria + lock view (unlock button + reset button con title/desc swap manejado por JS via `_refreshAndShow` con accountManager.lock.{title/desc} vs accountManager.lock.{setupTitle/setupDesc}) + list view toolbar (search placeholder + 3 filter selects con 4 status options + Export/Import/New buttons + 5 column headers + empty + Lock vault footer button) + editor view (8 form labels con placeholders + Back/Save buttons + 3 status options) + import view (4 mode radios con strong title + small desc + Cancel/Import buttons). Status options reusan keys del list scope (statusActive/NeedsRelogin/Inactive) entre filter dropdown y editor select. Import-intro `<p>Importing from <strong>filename</strong>. Choose...</p>` se renderiza via `_renderImportIntro()` con innerHTML desde `import.filenameIntro` con `{{filename}}` interpolation (escapado defensivo via String.replace). account-manager.js dinámico con local t() helper: `_refreshAndShow` traduce errorStatus + lock title/desc swap; `_reloadAndRender` errorLoadAccounts; `_doUnlock/Lock/Destroy` busy states + confirms + errors; `_populateFilters` traduce filterAllIdentities/Workspaces; `_renderList` plural-aware counts (countsZero / countsSingular con `{{filtered}}` / countsPlural con `{{filtered}}`/`{{total}}`); `_confirmDelete` con `{{username}}`/`{{site}}`; `_populateEditorSelects` placeholderWorkspaceNone; `_submitEditor` errorEditorRequired + saveFailed; `_doExport` exportFailed + exportAlert con `{{n}}`/`{{path}}`; `_doImportPick` renderImportIntro; `_doImportConfirm` errorNoFile + confirmOverwriteTotal + 2 busy/reset labels + errorImportFailed + summary header con `{{mode}}`/`{{n}}` + summaryIdentitiesCreated/summaryWorkspacesCreated con `{{list}}`. account-manager-render.js helpers: `statusBadge` mapea backend enum (active/needs_relogin/inactive) a `accountManager.statusBadge.*`; `actionsCell` traduce Edit/Delete tooltips. Constructor wirea `window.OZ.i18n.onChange()` → `_refreshAndShow()` si modal abierto + `_renderImportIntro()` si import view activa con archivo cargado. Catalog adds `accountManager.*` namespace (77 leaf keys EN+ES con sub-namespaces lock/list/editor/import/statusBadge/actions/errors/confirms). ES uses LATAM-neutral (Gestor de cuentas / Bóveda bloqueada / Llavero de macOS / Resetear / Combinación permanente / Sobrescritura total / Importando desde). Patch bump 1.5.11 → 1.5.12 + manifest WebUI bump. Tests verde. Lint clean. Prettier clean. check:loc 499. **🎉 Modal grande final completado.** Coverage remaining: proxy-dashboard.html partial, main-side command labels.

- [`2026-05-16`] [`i18n-bulk-opener / v1.5.11`] **i18n coverage — Bulk Opener modal (2 modos: existing/create)** (1 commit, cero deps, cero tests nuevos, +39 keys EN/ES). Patch bump. bulk-opener `oz-bo-modal`: data-i18n attrs en title + closeAria + 2 mode buttons + searchPlaceholder + selectVisible + 2 urlPatternLabel + 2 urlPatternPlaceholder + count/color/namePattern labels + previewLabel + targetLabel + newWorkspaceLabel + newWsPlaceholder + cancel button. URL hint con inline `<code>{n}</code>`/`<code>{i}</code>` renderizado via `_applyUrlHint()` con `innerHTML` desde `bulkOpener.existing.urlHintHtml`. bulk-opener.js dinámico con local t() helper: `_setMode()` traduce submit button (submitExisting vs submitCreate); `_renderExistingList()` traduce empty search; `_renderTargetOptions()` traduce currentWsOption con `{{name}}` + newWsOption + newWorkspaceDefault con `{{stamp}}` interpolation; `_updateCount()` traduce countSelected con `{{n}}`; `_renderPreview()` traduce previewEmpty; `_submit()` traduce errorCountRange + errorGeneric + create.namePatternDefault como fallback de el name pattern; `_showResult()` traduce plural-aware opened (singular/plural) + skipped (singular/plural) + 3 wsLine variants (wsCreated con `{{id}}` / wsUsed con `{{id}}` / wsCurrent) + errorRowFallback con `{{n}}` + moreErrors con `{{n}}`. Locale-aware name pattern default seteado UNA VEZ en constructor (`this.$namePattern.value = t(...)`) — user edits preservadas en subsequent opens. Constructor wirea `window.OZ.i18n.onChange()` → re-aplica urlHint + `_setMode/_renderTargetOptions/_renderExistingList/_renderPreview` si modal abierto + limpia result block (avoid cross-locale drift en data stale). Catalog adds `bulkOpener.*` namespace (39 leaf keys EN+ES con sub-namespaces existing/create/result). ES uses LATAM-neutral (Abrir en masa / Cuenta / Cantidad / Crear N nuevas / Espacio destino / Actual / Nuevo espacio). Patch bump 1.5.10 → 1.5.11 + manifest WebUI bump. Tests verde. Lint clean. Prettier clean. check:loc 499. Coverage remaining: account-manager (~50 strings — el grande), proxy-dashboard.html partial, main-side command labels.

- [`2026-05-16`] [`i18n-team / v1.5.10`] **i18n coverage — Team modal (Curve25519 key sharing, 4 vistas)** (1 commit, cero deps, cero tests nuevos, +44 keys EN/ES). Patch bump. team `oz-team-modal`: 4 vistas (notConfigured / standalone / owner / member). data-i18n attrs en title + closeAria + headers de cada vista + buttons + section titles + invite-display small + placeholder. Standalone view description con `<br /><br />` + `<code>/Apps/OZ Browser/</code>` se renderiza via `_applyStandaloneDesc()` con `innerHTML` desde la catalog key `descHtml` (data-i18n textContent strippearía el `<code>`). team.js dinámico con local t() helper: `_refresh()` traduce owner+member meta lines con `{{teamId}}/{{myMemberId}}` y `{{teamId}}/{{when}}/{{ownerMemberId}}` interpolation; `_loadMembers()` empty + member-row helpers (meSuffix, keyShared, waitingForKey, removeBtn); 6 confirm dialogs (create/join/leave/disband/removeMember + joinedAlert); 4 busy state labels (creating/joining/joinBtn/createBtn reset); PENDING invite info con `{{msg}}` interpolation; copy-invite info; 8 errors (status/loadMembers/create/invite/joinNoToken/joinDefault/leave/disband/removeMember). Constructor wirea `window.OZ.i18n.onChange()` → `_applyStandaloneDesc()` + `_refresh()` si modal abierto. Catalog adds `team.*` namespace (44 leaf keys EN+ES con sub-namespaces notConfigured/standalone/owner/member/members). ES uses LATAM-neutral (Equipo / Crear equipo / Unirme / Disolver / "vos" para meSuffix / "Sos el owner"). Out of scope: `oz-team-disconnected-view` no existe (la mention 1) "disconnected" en el comment de team.js es legacy — solo 4 vistas reales). Patch bump 1.5.9 → 1.5.10 + manifest WebUI bump. Tests verde. Lint clean. Prettier clean. check:loc 499. Coverage remaining: account-manager (~50 strings — el grande), bulk-opener, proxy-dashboard.html partial, main-side command labels.

- [`2026-05-16`] [`i18n-command-palette / v1.5.9`] **i18n coverage — Command Palette (Cmd+K)** (1 commit, cero deps, cero tests nuevos, +11 keys EN/ES). Patch bump. command-palette `oz-cmdk-modal`: data-i18n-attr en input `placeholder + aria-label`, data-i18n en `empty` + 3 footer kbd hints (navigate/select/close — wrapped en `<span>` para que el `<kbd>` sibling no se trague del textContent replacement). command-palette.js dinámico con local t() helper: `categoryLabel(type)` ahora pulls from `commandPalette.categories.*` (5 keys: actions/tabs/identities/workspaces/other), `new-identity` executor crea con name `t('commandPalette.newIdentityDefaultName')`. Constructor wirea `window.OZ.i18n.onChange()` → llama `_render()` si la paleta está abierta (re-render headers + rows reusan los textContent del innerHTML build). Catalog adds `commandPalette.*` namespace (11 leaf keys con sub-namespace `categories.*`). ES uses LATAM-neutral (Paleta de comandos / Pestañas / Sin resultados / Nueva identidad). **Out of scope**: las labels de los commands en sí (Settings, Time Machine, etc.) vienen del main process via `window.oz.commands.list()` — para localizarlas hay que tocar `browser/command-palette.js` también (sub-patch futuro). Patch bump 1.5.8 → 1.5.9 + manifest WebUI bump. Tests verde. Lint clean. Prettier clean. check:loc 499. Coverage remaining: account-manager (~50 strings — el grande), team, bulk-opener, proxy-dashboard.html partial, main-side command labels.

- [`2026-05-16`] [`i18n-cloud-backup / v1.5.8`] **i18n coverage — Cloud Backup modal (Dropbox)** (1 commit, cero deps, cero tests nuevos, +44 keys EN/ES). Patch bump. cloud-backup `oz-cb-modal`: data-i18n attrs en title + closeAria + 2 vistas (disconnected: title + desc1 + desc2 + connectBtn — connected: account/device labels + disconnect/refresh buttons + toggle text + toggle small desc + devicesTitle + devicesEmpty). `desc` original con `<br /><br />` separado en 2 `<p>` para que sean keys puros sin HTML inline. Last-upload pill SIN data-i18n (lo maneja JS y se re-render en onChange). cloud-backup.js dinámico con local t() helper: `_refreshAll` traduce 3 estados del lastUpload (noUploadsYet / lastUploadOk con `{{when}}` / lastUploadError con `{{error}}`); `_doConnect/Disconnect/ToggleAuto` busy states + errors + connectInfo; `_renderDeviceCard` traduce thisDeviceSuffix + plural-aware snapshots count (snapshotsSingular/Plural con `{{n}}`) + opcional latestSuffix con `{{when}}` + toggle button (hideSnapshots/browseSnapshots) + empty state; `_renderSnapshotRow` traduce button titles + restoreText; `_doRestore` traduce sourceLabel (Other con `{{folder}}` / Self) + confirm dialog con `{{id}}/{{source}}` interpolation + success alert con `{{source}}/{{preId}}` + restore-with-preId error con `{{msg}}/{{preId}}`; `_doDelete` traduce confirm con `{{id}}`. Constructor wirea `window.OZ.i18n.onChange()` → llama `_refreshAll()` si modal abierto, re-renderizando device cards + snapshot rows + status pill. Catalog adds `cloudBackup.*` namespace (44 leaf keys EN+ES con sub-namespaces disconnected/actions). ES uses LATAM-neutral phrasing (Respaldo en la nube / Conectar / Llavero / "este/otro dispositivo"). webui.html script order ya correcto. Patch bump 1.5.7 → 1.5.8 + manifest WebUI bump. Tests verde. Lint clean. Prettier clean. check:loc 499. Coverage remaining: account-manager (~50 strings — el grande), team, command-palette, bulk-opener, proxy-dashboard.html partial.

- [`2026-05-16`] [`i18n-time-machine / v1.5.7`] **i18n coverage — Time Machine modal (snapshots / restore / retention)** (1 commit, cero deps, cero tests nuevos, +40 keys EN/ES). Patch bump. time-machine `oz-tm-modal`: data-i18n attrs en title + closeAria + 2 vistas (locked: title/desc/unlockBtn — list: snapshot-now / cloud-backup / run-retention buttons + 4 column headers + empty message + shortcut hint). Summary span SIN data-i18n (lo maneja JS y se re-render en onChange). time-machine.js dinámico con local t() helper: `_doUnlock/SnapshotNow/Retention/Restore/Delete` traducen busy states (Unlocking…, Snapshotting…), confirm dialogs (`{{label}}` + `{{when}}` interpolation), retention applied alert con `{{n}}`, restoreCompleted alert con `{{preRestoreId}}`, restore-with-preId error con `{{msg}}/{{preId}}`. Nuevo helper `reasonLabel(reason)` mapea backend kebab-case (manual / pre-quit / pre-overwrite-total / pre-restore / daily-3am) a leaf key i18n (`timeMachine.reasons.*`) con fallback a `unknown`. `_renderRow` traduce: row sub-text "N files" via `rowFiles {{n}}`, reason badge via `reasonLabel`, button titles (Restore / Delete). `_renderList` calcula summary singular vs plural via 2 keys distintos (summarySingular / summaryPlural con `{{size}}`/`{{n}}`). Constructor wirea `window.OZ.i18n.onChange()` → re-renderiza la lista si el modal está abierto en la vista list (locked view es 100% data-i18n). Catalog adds `timeMachine.*` namespace (40 leaf keys EN+ES con sub-namespaces locked/col/reasons/actions). ES uses LATAM-neutral (Bóveda / instantánea→snapshot / "antes de…" para reasons). Patch bump 1.5.6 → 1.5.7 + manifest WebUI bump. Tests verde. Lint clean. Prettier clean. check:loc 499. Coverage remaining: account-manager (~50 strings), cloud-backup, team, command-palette, bulk-opener, proxy-dashboard.html partial.

- [`2026-05-16`] [`i18n-browsing-data / v1.5.6`] **i18n coverage — Browsing Data modal (3 tabs: bookmarks / history / downloads)** (1 commit, cero deps, cero tests nuevos, +25 keys EN/ES). Patch bump. browsing-data `oz-bd-modal`: data-i18n attrs en title + closeAria + tab labels (3) + search placeholders (2) + filter `All identities` option (3 places) + filter `All states` option + 4 download state options + empty messages (3) + `Clear all` button (3 places). browsing-data.js dinámico con local t() helper: `refreshIdentities()` traduce option "All identities" en innerHTML rebuild; `renderBookmarks/History/Downloads()` traducen el botón "Delete" en cada row + `stateLabel(state)` helper para mapear download state al label localizado; `handleClear*` confirms con `{{n}}` interpolation; standalone `timeAgo(ms)` helper migrado (justNow/minutes/hours/days con `{{n}}`). Listener `window.OZ.i18n.onChange()` re-renderiza identities filter + 3 listas si el modal está abierto al switch. Catalog adds `browsingData.*` namespace (25 leaf keys EN+ES con sub-namespaces `tabs.*`, `states.*`, `timeAgo.*`). ES uses LATAM-neutral phrasing consistent con resto del catalog. Script order ya correcto (browsing-data.js carga después de i18n.js). Patch bump 1.5.5 → 1.5.6 + manifest WebUI bump. Tests verde. Lint clean. Prettier clean. check:loc 499. Coverage remaining: account-manager (~50 strings), time-machine, cloud-backup, team, command-palette, bulk-opener, proxy-dashboard.html partial.

- [`2026-05-16`] [`i18n-identity-editor / v1.5.5`] **i18n coverage — Identity Editor modal** (1 commit, cero deps, cero tests nuevos, +14 keys EN/ES). Patch bump. identity-editor `oz-identity-modal`: data-i18n attrs en title + closeAria + nameLabel + colorLabel + uaLabel + uaUseDefault button + cancel + save; data-i18n-attr en textarea placeholder. identity-editor.js dinámico con local t() helper: `titleWithName` con `{{name}}` interpolation, `uaHintHtml` (mantiene HTML inline con `<strong>` y `<code>` para fingerprint disclaimer), `uaHintDefaultIdentity` para identity Default que no permite UA custom, `errorNameRequired` + `errorSaveFailed`. Nuevo método `_applyUaHint()` reemplaza el `dataset.originalText` snapshot pattern — la fuente de verdad ahora es el catalog. Listener `window.OZ.i18n.onChange()` re-renderiza titleWithName + uaHint en locale switch sin cerrar el modal. Catalog adds `identityEditor.*` namespace (14 keys EN+ES). Script reorder en `webui.html`: identity-editor.js movido a DESPUÉS de i18n.js (su constructor ahora depende de window.OZ.i18n). Patch bump 1.5.4 → 1.5.5 + manifest WebUI bump. Tests verde. Lint clean. check:loc 499. Coverage remaining: account-manager (~50 strings), time-machine, cloud-backup, team, browsing-data, command-palette, bulk-opener, proxy-dashboard.html partial.

- [`2026-05-16`] [`i18n-identity-clone / v1.5.4`] **i18n coverage — Identity Clone modal** (1 commit, cero deps, cero tests nuevos, +~23 keys EN/ES). Patch bump. identity-clone `oz-clone-modal`: data-i18n attrs en title + closeAria + cloningFrom + newNameLabel + newNamePlaceholder + legend + 3 checkbox titles + cancel + clone. identity-clone.js dinámico con local t() helper: uaHintCurrent con `{{ua}}` interpolation, uaHintNone, proxyHintDefault, 6 error messages (NoSource, NotFound, EmptyName, NoSrcId, CapReached, CloneFailed con `{{reason}}`). Catalog adds `identityClone.*` namespace (23 keys EN+ES). Patch bump 1.5.3 → 1.5.4. Tests verde. Lint clean. check:loc 499. Coverage remaining: account-manager (~50 strings), time-machine (~30), cloud-backup (~25), team (~25), proxy-dashboard.html partial.

- [`2026-05-16`] [`i18n-extensions-modal / v1.5.3`] **i18n coverage — Extensions per-identity modal** (1 commit, cero deps, cero tests nuevos, +~11 keys EN/ES). Patch bump. extensions-modal `oz-ext-modal`: data-i18n attrs en title + closeAria + identityLabel + empty state + Done button. extensions-modal.js dinámico con local t() helper para: identity select option Default suffix `{{name}} (Default)`, `alwaysEnabled` tag, `enabled`/`disabled` checkbox labels, `errorNoIdentities`, `errorToggleFailed` con `{{reason}}` interpolation. Catalog adds `extensionsModal.*` namespace (11 keys). EN/ES sentence completos. Patch bump 1.5.2 → 1.5.3. Tests verde. Lint clean. check:loc 499. Coverage remaining: account-manager, time-machine, cloud-backup, team, proxy-dashboard.html, identity-clone.

- [`2026-05-16`] [`i18n-health-modal / v1.5.2`] **i18n coverage — Anti-Detect Health modal (4 vectors + overall + status pills + fix actions)** (1 commit, cero deps, cero tests nuevos, +~18 keys EN/ES). Patch bump. Health-modal `oz-health-modal`: data-i18n attributes en title + identityPrefix + Re-check + Close + closeAria. health-modal.js dinámico: helper local t() para `vectorLabel(key)` (4 vectors: IP↔Timezone / Fingerprint coherence / Cookie health / Proxy reachability) + `statusLabel(status)` (4 statuses: OK/Warning/Critical/Unknown) + overall pill con `{{status}}` interpolation + working state button + errorFixFailed con `{{reason}}` + errorNoIdentity + errorLoadFailed. Catalog adds `healthModal.*` namespace con sub-namespaces `status.*` (4 keys) + `vectors.*` (4 keys) + 10 top-level. EN/ES sentence completos. Coverage remaining (v1.5.x patches): account-manager modal, time-machine, cloud-backup, team, extensions-modal, proxy-dashboard. Patch bump 1.5.1 → 1.5.2. Tests verde. Lint clean. check:loc 499.

- [`2026-05-16`] [`i18n-notifications / v1.5.1`] **i18n coverage — notifications modal + dynamic toast strings (timeAgo, action labels, stats)** (1 commit, cero deps, cero tests nuevos, +~16 keys EN/ES). Patch bump. Notifications panel `oz-notif-modal`: data-i18n attributes en title + Mark all read + Clear all buttons + empty state. notifications.js dinámico: helper local t() para `timeAgo` (just now/Nm/Nh/Nd ago con interpolation {{n}}) + `actionLabel()` switch (Open Accounts/Time Machine/Proxies/Settings/Browsing Data/Identity/Tab) + `clearAllConfirm` + `stats` con `{{total}}/{{unread}}` interpolation. Catalog adds `notifications.*` namespace con sub-namespaces `timeAgo.*` (4 keys) + `actions.*` (8 keys) + 6 top-level. EN/ES sentence completos. Coverage gaps remaining (v1.5.x patches): proxy-dashboard.html, account-manager modal, health-modal, time-machine, cloud-backup, team, extensions-modal — cada uno su propio sub-bloque. Tests verde. Lint clean. check:loc 499. Version bumps 1.5.0 → 1.5.1.

- [`2026-05-16`] [`i18n-welcome-wizard / v1.5.0`] **i18n coverage — welcome modal (3 screens) + onboarding wizard (5 steps) fully translated EN/ES** (1 commit, cero deps, cero tests nuevos, +~80 keys EN + ~80 keys ES). **Minor bump 1.5.0** (pre-approved en roadmap v1 para i18n coverage chunk). Welcome modal `oz-onb-modal`: agregar `data-i18n` attributes a todos los strings (3 screens × 1 title + 1 tagline + 3 list items each = 21 strings + 4 buttons). Onboarding wizard `oz-wiz-modal` (dinámico JS-rendered): refactor de `_renderXStep()` methods para usar `t('wizard.stepN.field')` en template literals + interpolation `t(key, {n: count})`. Helper `t()` con fallback to-key-if-i18n-not-loaded para que el wizard nunca rompa si carga antes que i18n. **Coverage gaps explícitos** (sub-bloque v1.5.x patches): notifications.js toast messages, error messages dinámicos (per-modal), proxy-dashboard.html, account-manager, health-modal, time-machine, cloud-backup, team, extensions-modal. ~50+ surfaces remain hardcoded EN (future patches). **Catalog structure**: `welcome.identities/workspaces/antidetect.{name,desc}` para los 3 screens, `wizard.step1-5.*` per-step. Interpolation strings con `{{n}}/{{prefix}}/{{workspace}}/{{ok}}/{{fail}}` etc. EN/ES con sentence completos (no fragmentos sueltos). **Version bumps** package + manifest WebUI 1.4.7 → 1.5.0 (minor — i18n coverage feature pre-aprobada en v1 roadmap). Tests verde (suite full ~3349). Lint clean (4 pre-existing). check:loc 499.

- [`2026-05-15`] [`K1-onboarding-wizard / v1.4.6`] **5-step action wizard — último K1-extra, cierra v1.4.x** (1 commit, cero deps nuevas, cero tests nuevos para el wizard pero +0 cambios en suite). Wizard guiado que arma el setup core de OZ para un new user en 5 pasos: (1) Workspace — crea workspace con nombre + color, (2) Proxies — bulk import paste host:port:user:pass via existing proxyImporter, (3) Identities — crea N identities en el workspace via existing identities.create, (4) Asignar — 1:1 pairing via existing proxyBulkAssign, (5) Test — proxyHealth.testAllAndStatus() reporta ok/fail/total. **Reusa primitives existentes** (workspaces, proxyImporter, identities, proxyBulkAssign, proxyHealth) — el wizard es solo el orchestration layer con progress dots + Skip/Back/Continue. **Trigger**: auto-launch post-welcome si `settings.onboardingWizard.completed === false`. Flag independiente del `settings.onboarding` (welcome 3-screen info-only del 1.10c) — un user puede haber visto el welcome y re-launch el wizard desde Settings sin re-ver el welcome. **Backend** `settings-manager.js` extiende DEFAULTS con sección `onboardingWizard: {completed, skippedAt, skippedAtStep}` + nuevo validateKey case para `skippedAtStep` (small non-negative integer or null). **Frontend** `browser/ui/onboarding-wizard.js` (~330 LOC, single class `OnboardingWizard` con render dispatch per step + state machine). Each step's \_doX() async invoca el IPC correspondiente y retorna boolean (true = advance, false = stay). Step 5 \_doTestStep() corre testAllAndStatus → setea state.testResults → re-render shows green/red summary. HTML markup `oz-wiz-modal` agregado a `webui.html` después del existing `oz-onb-modal` — reusa CSS classes existentes (`.onb-screen`, `.onb-dot`, `.onb-controls`). CSS nuevo ~80 líneas para `.wiz-step`, `.wiz-field`, `.wiz-textarea`, `.wiz-status`, `.wiz-hint`, `.wiz-summary` (no override existing `.onb-*` clases). webui.js instancia `OnboardingWizard` y al boot lanza `wizard.open()` si flag no completed. **UX flow per step**: form mínimo con validation inline, "Continue" button llama \_doStep que ejecuta el action (workspace.create / proxyImporter.import / identities.create x N / proxyBulkAssign.execute / proxyHealth.testAllAndStatus). On success, advance al next step + state carry-over (workspaceId → step 3, proxyIds + identityIds → step 4). Skip button cierra wizard + marca completed con skippedAtStep para analytics. **Defensive**: Skip step 2 si user no pega nada (proxies opcional), Skip step 4 si no hay proxies/identities (nothing to pair). **Settings UI**: pending — Jose puede re-launch el wizard desde la console `window.ozOnboardingWizard.open()` mientras tanto. **Version bumps** package + manifest WebUI 1.4.5 → 1.4.6 (patch). Lint clean (4 issues pre-existing). check:loc 499. Tests acumulados ~3349 (cero cambios — wizard tests pending; settings tests verde con el nuevo section). **🎉 K1-extras CERRADO 5/5**: bulk-open ✅ + session-warmer ✅ + mac-sleep ✅ + identity-hud ✅ + onboarding-wizard ✅. **Próximo**: smoke visual real del wizard end-to-end + 1.5.0 i18n full + I Apple signing (bloqueado approval).

- [`2026-05-15`] [`fp-hook-order-fix / v1.4.5`] **CRITICAL: FP anti-detect engine restored end-to-end — UA + platform + languages + hw now applied correctamente per-identity** (1 commit, cero tests nuevos, cero deps, ~10 line change pero MASSIVE behavioral fix).

  **Bug:** descubierto durante smoke visual del bundle fix (v1.4.4): aunque el bundle hizo que preload-fingerprint.js cargara sin error, el FP UA override seguía sin aplicarse. Page console mostraba `Mozilla/5.0 (Macintosh) ... OZBrowser/1.4.4 ... Electron/42.0.1` en lugar del FP-generated UA. Root cause investigation reveló que `AntiLogout.install()` corría DURANTE el init() ANTES de que `setupFingerprintPreload(this)` + `setupHud(this)` registraran sus `identityManager.addSessionInitHook()` callbacks. AntiLogout.install() itera `identityManager.list()` (12 identities en setup de Jose) y llama `identityManager.getSession(id)` para cada una — eso CACHEA las sessions sin que los hooks de FP + HUD hayan fired. Después de eso, cuando los tabs se materializan, las sessions vienen del cache y los hooks NO se ejecutan (`getSession` retorna early si `sessionCache.has(id)`).

  **Fix:** defer `this.antiLogout.install()` a DESPUÉS de `setupFingerprintPreload(this)` + `setupHud(this)`. Cambio mínimo en main.js (~10 líneas movidas + comment explicando el por qué). AntiLogout constructor sigue donde estaba (no hace side effects). `install()` que ES el que toca sessions corre al final.

  **Smoke visual 2026-05-15 PASS end-to-end** en la IG 2 tab (identityId `8ffa1008da6a795f` con FP profile pt-BR Linux):
  - `navigator.userAgent` → `Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/135.0.0.0 Safari/537.36` ✅ (NO MORE OZBrowser/Electron)
  - `navigator.platform` → `Linux x86_64` ✅ (override via webFrame.executeJavaScript en preload-fingerprint.js)
  - `navigator.languages` → `['pt-BR', 'pt', 'en']` ✅
  - `navigator.hardwareConcurrency` → `12` ✅
  - **Instagram automáticamente sirvió la página en portugués** ("Espere!", "Perfil não está disponível", "Cadastrar-se no Instagram") — confirma que el Accept-Language header coherent con el navigator.languages está siendo enviado en cada request.

  **Boot log evidence**: `[fingerprint-preload-setup] session UA set from FP {identityId, ua, language}` aparece **12 veces** (una per identity hooked por AntiLogout) seguidas inmediatamente por `[anti-logout] cookie hook installed {identityId, hostsCount:32}` — confirma que el orden es FP_hook → AntiLogout_per_identity.

  **Why this was silently broken for so long:** el FP nunca tuvo smoke visual real (changelog v1.1.4 menciona "Smoke visual REAL pendiente con app corriendo"). El bundle fix v1.4.4 también surface'd este bug porque sin el bundle fix, los preloads no cargaban en absoluto, lo que daba un símptoma diferente y oscurecía este reorder issue.

  **Impacto en producto:** **anti-detect engine ahora opera por primera vez en este build.** Las 12 identidades de Jose generan fingerprints únicos consistentes per-identity:
  - IG 2 → Linux Chrome 135 pt-BR
  - 4c6c37aac35648ea → Windows Edge 135 fr-FR
  - 1064b87a6d8b2263 → Windows Edge 135 fr-FR
  - 08f29d3f97bc9a79 → Linux Chrome 135 de-DE
  - 2e270e3b032df21f → Mac Chrome 135 ja-JP
  - … (etc, total 12 con blueprints variados)

  Cada identity tiene UA + platform + languages + timezone + screen + WebGL vendor/renderer + canvas noise + hardware coherentes. Anti-detect tools como Pixelscan, BrowserLeaks, CreepJS deberían ahora pasar (smoke visual posterior pendiente).

  **Version bumps** package + manifest WebUI 1.4.4 → 1.4.5 (patch). Lint clean. check:loc 499. Tests acumulados ~3349 (cero cambios — pure runtime fix, todos los unit tests del FP engine ya pasaban). Smoke visual PASS.

  **Pendiente** (sub-bloques separados):
  1. Onboarding wizard 5-step — último K1-extra, cierra v1.4.x.
  2. Smoke visual anti-detect tools (Pixelscan / BrowserLeaks / CreepJS) — verificar que el coherence score sube.
  3. Re-test auto-fill + TOTP con preload-content bundled (Bug del v1.4.4 también se resolvió por el bundle, sub-test pendiente).

- [`2026-05-15`] [`sandbox-preload-fix / v1.4.4`] **Fix critical: bundle content preloads via webpack para sortear sandbox require() bug en Electron 42** (1 commit, cero deps nuevas — webpack ya transitive de @electron-forge/plugin-webpack).

  **Bug:** sandboxed preloads de Electron 42 fallan silenciosamente cuando hacen `require()` de archivos siblings (descubierto durante smoke visual de Identity HUD v1.4.3). Afecta a **`preload-content.js`** (auto-fill + auto-save + TOTP inject del bloque J auto-login) Y **`preload-fingerprint.js`** (FP UA override + canvas/audio/webgl spoofs del 1.9b). Visible en page console como `Unable to load preload script ...preload-content.js / Error: module not found: ./site-templates`. Sin estos preloads, las features core del producto (auto-login a las 50 IG accounts + anti-detect fingerprint) están silentemente rotas en el build dev.

  **Fix:** nuevo `scripts/bundle-preloads.js` (~100 LOC) que invoca webpack programáticamente para bundlear cada content preload + sus deps relativos en un solo `.js` standalone. Output: `browser/.bundled/<name>.bundled.js`. El bundle resuelve los requires en build-time, así el archivo final SÓLO contiene `require("electron")` (whitelisted en sandbox). Config webpack: `target: 'node'`, `libraryTarget: 'commonjs2'`, `iife: true`, `externals: { electron: 'commonjs2 electron' }`, `splitChunks: false` (single self-contained file per preload).

  **Wire-up:** `identity-manager.js` `contentPreloadPath()` y `fingerprint-preload-setup.js` `fpPreloadPath` ahora apuntan a `app.getAppPath() + /browser/.bundled/<name>.bundled.js` en lugar del source. **npm scripts** `prestart` + `prepackage` + `bundle:preloads` (manual) corren el script automáticamente — sin overhead para Jose. `.gitignore` agrega `browser/.bundled/`. `forge.config.js` `extraResource` agrega `browser/.bundled` para que el packaging incluya los bundles.

  **Performance:** ~1-2s por bundle, ~3s total post-prestart. Sin cambios para `npm test` o lint.

  **Bundle sizes:** preload-content.bundled.js ~22.2 KiB (inlinea site-templates 314 LOC) + preload-fingerprint.bundled.js ~15.3 KiB (inlinea preload-fingerprint-script 266 LOC). Auto-stamped con header `// AUTO-GENERATED ...`.

  **Smoke visual 2026-05-15 PASS** (parcial): page console ya NO muestra `Unable to load preload script ...preload-content.js`. Verificado en https://www.instagram.com/* con identity IG 2. **El UA override del FP engine NO se está aplicando todavía** (UA muestra `OZBrowser/1.4.4 Chrome/148.0.7778.97 Electron/42.0.1` en lugar del FP-generated UA) — sub-bloque pendiente para investigar el identity-resolution path del FP hook (separado del bundle issue que este commit cierra). El HUD del 1.4.3 sigue funcionando perfecto.

  **Version bumps** package + manifest WebUI 1.4.3 → 1.4.4 (patch). Lint clean. check:loc 499. Tests acumulados ~3349 → 3349 (cero cambios — el script de bundle no agrega asserts, solo build-tooling).

  **Pendiente** (sub-bloques separados):
  1. Investigar por qué FP UA override no se aplica aunque el preload ahora carga (`session.setUserAgent` desde el FP hook + `webFrame.executeJavaScript` con el override script).
  2. Onboarding wizard 5-step — último K1-extra.

- [`2026-05-15`] [`K1-identity-hud / v1.4.3`] **In-page identity HUD widget arriba-derecha en cada tab — smoke visual PASS end-to-end** (1 commit, **+122 tests**, cero deps nuevas).

  **Resumen ejecutivo:** widget shadow-DOM arriba-derecha en cada tab con identity name + color badge + workspace + proxy country flag (🇲🇽 🇺🇸) + IP last octets ofuscados (·144.18 para IPv4) + session health pill (green/amber/red/gray). Expanded por default, collapse a pill mini per-identity persistente via localStorage. Smoke visual 2026-05-15 confirmado: HUD aparece + click chevron colapsa + identity color de la IG 2 renderizada correctamente.

  **Arquitectura — pivot crítico durante smoke visual:** la primera implementación (commit pendiente, antes del pivot) usaba `session.registerPreloadScript` con un archivo preload-hud.js (mismo pattern que preload-fingerprint.js). El smoke visual reveló que **los sandboxed preloads de este build fallan silenciosamente cuando hacen require de archivos siblings** — mismo bug que afecta a preload-content.js (visible en page console como `Unable to load preload script ...preload-content.js / Error: module not found: ./site-templates`). El UA test confirmó que el FP TAMBIÉN está roto en este build (la UA muestra default OZBrowser sin override). **Pivot:** mover la inyección a `webContents.executeJavaScript()` desde main process al `did-finish-load` + `did-navigate-in-page` + initial sweep post-boot. Bypassa el sandbox totalmente, NO requiere preload registration, y el HUD es 100% data-injection (sin IPC desde la página). Trade-offs: collapse state persiste en localStorage del site en lugar de per-identity en main (soft regression aceptable para v1.4.3 — el último K1-extra, onboarding wizard, puede extender persistencia full si Jose lo pide).

  **Backend** `browser/hud-handlers.js` (~200 LOC) factory `buildHudHandlers(browser, {dataDir|app}) → {getContextForSession(senderSession, identityIdArg?), getContext(identityId), getCollapsed(identityId), setCollapsed(identityId, collapsed)}`. Combina IdentityManager.get + WorkspaceManager.get + ProxyAssignment.resolve + AccountVault session status en un blob `{identity:{id,name,color,isDefault}, workspace:{id,name,color}, proxy:{id,country,host,port,protocol,healthy}, session:{status}}`. Session status logic: vault locked → 'locked' gray / sin accounts → 'green' / ≥1 account.status==='needs_relogin' (anti-logout) → 'needs_relogin' red / else green. Status 'amber' reservado para futuro "cookie expira en <24h".

  **Pure builders** `browser/preload-hud-script.js` (~180 LOC, sin Electron — testeable directo) con `countryToFlag` (Regional Indicator Symbols, "MX"→🇲🇽), `ipLastOctets` (privacidad: /24 IPv4 ofuscado, hostname clamped a 12), `escapeHtml` (XSS defense), `badgeInitials`, `sessionPill`/`pillTitle`, `buildHudStyles` (CSS shadow scope), `buildExpandedHtml`/`buildCollapsedHtml`.

  **Setup + wire-up** `browser/hud-setup.js` (~245 LOC) wirea end-to-end: (1) registra `app.on('web-contents-created')` que en cada nuevo wc hookea `did-finish-load` + `did-navigate-in-page` + setTimeout fallback 200ms para tabs already-loaded. (2) `refreshHudOnTab(browser, wc)` resuelve identityId via 3 estrategias en cascada — tab lookup por wc reference (autoritativo, usa tab.identityId), session-based lookup (legacy), browser.activeIdentityId fallback. (3) `buildInjectionScript(ctx)` arma un IIFE que crea shadow-DOM en `document.body` y renderea con la data inlined como JSON literal. (4) Click handlers in-page leen/escriben `localStorage[oz_hud_collapsed_{identityId}]` y toggle render local sin round-trip al main. (5) Wrap de `browser.broadcastToWebUI` — channels del whitelist HUD_REFRESH_CHANNELS (identities/workspaces/accounts/proxies/proxyAssignment/proxyHealth :changed) re-ejecutan `executeJavaScript` en todos los tab webContents materializados con la data fresca.

  **Refactor incidental** (ADR 0005, main.js exactly 500 LOC budget post-K1-mac-sleep): el bloque de fingerprint preload hook se extrajo a `browser/fingerprint-preload-setup.js` factory `setupFingerprintPreload(browser)`. Cero cambios de lógica vs inline previo — sólo movimiento. main.js drop a ~485 LOC (de 500).

  **preload-hud.js** queda como placeholder documentado (no se puede borrar via sandbox — operation not permitted). Si querés removerlo, después del primer commit hacé `git rm browser/preload-hud.js` + quitarlo del `extraResource` de forge.config.js.

  **IPC** 3 channels: `oz:hud:getContext` (resuelve via event.sender.session), `oz:hud:getCollapsed`, `oz:hud:setCollapsed` (legacy del approach inicial — quedan para futuros consumers como MCP).

  **Tests** 2 archivos: `tests/hud-handlers.smoketest.js` (84 asserts: 60+ preload-hud-script puros, 15 hud-handlers context/session-status, 9 collapsed state persistence) + `tests/hud-setup.smoketest.js` (38 asserts: HUD_REFRESH_CHANNELS, shouldSkipUrl 8, buildInjectionScript 10, setupHud + refreshHudOnTab + broadcast wrap end-to-end). Fakes inyectables, NO toca Electron real.

  **Cómo lo experimenta Jose**: cada tab muestra un widget arriba-derecha. Hover sobre la pill → tooltip "Session healthy / Vault locked". Click en chevron → colapsa a pill mini con color+inicial+pill. Click pill → re-expande. State persiste per-identity per-site entre relaunches via localStorage. Cuando anti-logout flaggea needs_relogin → la pill cambia a rojo en TODOS los tabs de esa identity en tiempo real (sin reload del tab).

  **Version bumps** package + manifest WebUI 1.4.2 → 1.4.3 (patch). Lint clean (mis archivos; 4 issues pre-existing en otros archivos sin tocar). check:loc max 499. Tests acumulados ~3227 → **~3349**. **Pendiente K1-extras restantes** (1.4.x): onboarding wizard 5-step (~3h) — último K1-extra.

- [`2026-05-15`] [`K1-mac-sleep / v1.4.2`] **Re-test proxies on Mac wake from sleep — powerMonitor listener** (1 commit, **+11 tests**, cero deps nuevas). Nuevo `browser/power-monitor-setup.js` (~150 LOC) factory `setupPowerMonitor({proxyHealth, powerMonitor, settingsManager, debounceMs})` + helpers `wirePowerMonitorOntoBrowser` + `teardownPowerMonitorFromBrowser` (estos dos extraídos para mantener main.js ≤500 LOC). Listen `powerMonitor.on('resume')` → debounce 3000ms (coalesce rapid lid open/close cycles) → `proxyHealth.testAll()`. Use case: durante sleep, network state cambia (WiFi → wired, VPN dropped, provider rotates IPs); sin esto, el primer tab post-wake puede silently quedar con proxy muerto y el user ve "page not loading" sin contexto. **Setting opt-out** `notifications.macSleepProxyRescan = false` (default true) leído via existing settingsManager — toggle takes effect en runtime sin reboot. Logged en cada step (info para resume detected + retest start + retest done, warn para failures). Wire en main.js compact (2 lines): `wirePowerMonitorOntoBrowser(this)` post-proxyHealth.startDaemon + `teardownPowerMonitorFromBrowser(this)` en before-quit handler. **Tests** `tests/power-monitor-setup.smoketest.js` (~190 LOC, **11 asserts**): defensive guards (no proxyHealth → noop, no powerMonitor → noop), exports SETTING_KEY + DEFAULT_DEBOUNCE_MS, listener registration via fake powerMonitor.on, stop() removes listener, \_trigger fires testAll happy path, settings opt-out skips testAll, settings undefined defaults to true, debounce coalesces 3 rapid resume events → 1 testAll call, testAll throws → caught with `{ok:false, error}` sin rethrow. Inyecta fakes — NO toca Electron real. **Version bumps** package + manifest 1.4.1 → 1.4.2 (patch). Lint clean. check:loc max 500 (main.js exactly 500). Tests acumulados ~3216 → **~3227**. **Pendiente K1 restantes** (1.4.x): identity HUD widget (~3h), onboarding wizard 5-step (~3h). Smoke visual REAL pendiente — para validar Mac sleep manually: cerrar lid 30s, abrir, verificar logs `power-monitor-setup` resume + retest done.

- [`2026-05-15`] [`K1-session-warmer / v1.4.1`] **Session warmer Scheduled Action handler — keep social cookies fresh sin abrir tabs** (1 commit, **+16 tests**, cero deps nuevas). Nuevo `ACTION_SESSION_WARMER = 'session-warmer'` agregado al registry de scheduled-action-handlers.js (sumado a los existentes open-workspace + sync-push + backup-snapshot del Bloque F). Use case core: el setup de Jose ("50 cuentas IG logueadas que se queden logueadas") sufre de session expiry por inactividad — plataformas como IG/X/FB rotan session cookies en cada request server-side, y sin tráfico real las cookies vencen. Anti-logout extiende cookies localmente pero algunos providers solo refrescan al ver requests reales. **Design**: lightweight HTTP touch (NO BrowserWindows — overkill para cron). Para cada identity en `params.workspaceId` OR `params.identityIds` (cap 50), resolver URL (priority: `params.urlsBySite[account.site]` → derived `https://<account.site>/` from accountVault → `params.fallbackUrl`), abrir `net.request` via `session.fromPartition('persist:identity-<id>')` con `useSessionCookies:true` y throttle 1s entre identities. Timeout 8s por request. Returns `{warmed:[{identityId,url,status}], skipped:[{identityId,reason:'no-url'}], errors:[{identityId,url,message}], totalRequested}`. Locked vault → `{skipped:true, reason:'vault-locked'}` (paridad con otros handlers F). **Wire** en `scheduled-setup._buildDeps`: si `browser.identityManager` está presente, pasa identityManager + (optional) workspaceManager + accountVault → `registerScheduledActionHandlers` registra el handler automáticamente. UI/Scheduled Actions panel ya soporta agregar acciones de cualquier tipo registrado — sin cambios necesarios ahí. **Tests** `tests/session-warmer.smoketest.js` (~220 LOC, **16 asserts**): factory guards (throws sin identityManager, ACTION*TYPES contiene 'session-warmer'), locked vault skip, identityIds resolution from workspaceId con orden + status correctos, throttle ≥ 1000ms × N enforced (real setTimeout), accountVault → URL derivation via account.site → homepage URL, urlsBySite override, identidades sin accounts → skipped no-url, fallbackUrl, BAD_PARAMS sin workspaceId ni identityIds. Inyecta fakes: identityManager / workspaceManager / accountVault / sessionFactory / netRequest — NO toca Electron real. **Cómo lo usa Jose**: Scheduled Actions panel → "+ Add action" → type='session-warmer', params=`{workspaceId:'<insta-ws>'}` (o `identityIds:[...]`), cron='0 */6 \_ \* \*' (cada 6h). Daemon fire → todas las identities del workspace reciben un GET a su site primary → cookies se refrescan server-side → identities siguen logueadas. **Version bumps** package + manifest 1.4.0 → 1.4.1 (patch). Lint clean. check:loc max 499. Tests acumulados ~3200 → **~3216**. **Pendiente K1 restantes** (1.4.x): identity HUD widget (~3h), onboarding wizard 5-step (~3h), Mac sleep/wake proxy re-scan (~2h). Smoke visual REAL pendiente.

- [`2026-05-15`] [`K1-bulk-open / v1.4.0`] **Bulk-open 1-click desde el workspace context menu** (1 commit, **+14 tests**, cero deps nuevas). Hallazgo durante exploración: el bulk-opener completo (backend `browser/bulk-opener.js` 265 LOC con `bulkOpenFromExisting` + `bulkCreateNew` + UI modal en `browser/ui/bulk-opener.js` + IPC + preload + integración command-palette) **ya vivía completo desde 1.5/C-4** — solo faltaba el 1-click shortcut desde el sidebar. **Cambios mínimos para v1.4.0**: nuevo menu item en `browser/workspace-context-menu.js` "Open all identities in tabs… (N)" con el count interpolado, enabled solo cuando workspace tiene >0 identities + no está frozen/archived. Click → `browser.broadcastToWebUI('oz:bulk-open:open', {mode:'existing', workspaceId, identityIds})`. `preload.js` extiende el listener `onOpen(cb)` para forwardear el payload (antes era no-args). `browser/ui/bulk-opener.js open(payload?)` ahora acepta el pre-fill: setea mode + pre-selecciona el Set de identityIds validados (filtra contra identities actuales por si el workspace cambió entre right-click y modal open) + setea activeWorkspaceId target. User flow: right-click workspace → "Open all identities…" → modal abre con todo seleccionado → user tipea URL → Go. De ~5 clicks a 2 clicks. **Tests** `tests/workspace-context-menu.smoketest.js` (~165 LOC, **archivo nuevo**, **14 asserts**): basic shape (template no vacío, contiene Rename/Duplicate/Freeze/Quick Tabs/Open all entries) + nueva entry expone count + enabled state (3 identities=enabled, 0 identities/frozen/archived=disabled) + click handler broadcasts payload correcto (channel `oz:bulk-open:open`, mode='existing', workspaceId, identityIds array) + edge cases (no workspaceManager → empty template, unknown wsId → disabled placeholder, default workspace tiene Open all entry pero NO Delete). Suite full verde. Lint clean. check:loc max 499. **Version bumps** package + manifest 1.3.0 → 1.4.0 (minor bump por K1 feature grande, pre-aprobado en roadmap). Tests acumulados ~3186 → **~3200** (+14). **Pendiente K1-extras restantes** (1.4.x): session warmer template Scheduled Actions (~2h), identity HUD widget arriba-derecha (~3h), onboarding wizard 5-step (~3h), Mac sleep/wake proxy re-scan (~2h). Smoke visual REAL pendiente.

- [`2026-05-15`] [`J / v1.3.0`] **Auto-login set completo (J-1 + J-2 ya en 1.5c + J-3 TOTP nuevo + J-4 auto-relogin loop)** (1 commit, **+17 tests**, cero deps nuevas — RFC 6238 implementado from scratch). **Minor bump** porque cierra el feature **core** que faltaba: "50 cuentas Insta logueadas que se queden logueadas". Hallazgo durante exploración: J-1 (auto-fill) y J-2 (auto-save) **ya vivían en 1.5c** (`browser/preload-content.js` 190 LOC + `browser/site-templates.js` 249 LOC con 10 plataformas: X/IG/FB/TikTok/LinkedIn/Google/Reddit/Threads/Telegram/Discord — `installAutoFill`, `installAutoSave`, `getCredentialsForSite`, `proposeAutoSave` IPC con identity resolution server-side anti-impersonation). Decisión Jose: saltarse G-6 (Ghost importer también importa proxies — su Ghost proxy list está vacío, cero valor) y arrancar J directo. **J-3 TOTP inject (greenfield)**: nuevo módulo `browser/totp.js` (~120 LOC, **pure, RFC 6238 from scratch** — HMAC-SHA1 + base32 decoder + dynamic truncation, sin deps externas). `generateTotp(secretBase32, {digits=6, stepSec=30, nowMs=Date.now()})` retorna el código rotativo. `decodeBase32` tolerante a whitespace/lowercase con padding opcional. `site-templates.js` extendido: las 4 plataformas críticas (Instagram, X, Facebook, Google) ahora tienen `totpUrlPatterns` (regex per platform para detectar la 2FA challenge URL) + `selectors.totpInput` (selector del input de código 2FA, con fallback a `autocomplete="one-time-code"` standard HTML). Nuevas funciones `isTotpUrl(url)` + `matchByTotpUrl(url)` exportadas. `preload-content.js` extendido con `installTotpFill()` mirror del autoFill pattern: trigger on DOMContentLoaded + SPA URL diff each 1.5s, match by template OR fallback a `autocomplete="one-time-code"` probe, IPC `oz:accounts:getTotpForSite(site, identityId)`, wait for selector + `setInputValue` con React-compatible event dispatch. **IPC nuevo** `oz:accounts:getTotpForSite` con identity resolution via `event.sender.session` (anti-impersonation, identityIdArg de renderer ignorado). **`account-handlers.getTotpForSite`** filtra accounts por (identityId, site) con `status !== 'inactive' AND totpSecret != null`, sort por lastLoginAt desc, genera TOTP en-process via `browser/totp.js` y retorna `{code, accountId}`. CRITICAL: el `totpSecret` NUNCA sale del main process — solo el código rotativo 6-digit cruza el IPC boundary. **J-4 Auto-relogin (minimal change)**: hallazgo que la mayor parte del loop YA estaba implementado por piezas existentes (anti-logout `_maybeFlagNeedsRelogin` con alertManager.add severity='urgent' + proposeAutoSave update path ya hacía `status:'active'` post-confirm). Lo único faltante: `getCredentialsForSite` ahora flipea optimistically `status:'needs_relogin' → 'active'` cuando auto-fill happens (anti-logout's cookie watcher re-flagea automáticamente si el fill no resultó en login exitoso). Retorna `wasNeedsRelogin:boolean` para preload/UI hints futuros + broadcast `oz:accounts:changed`. **Loop end-to-end**: anti-logout detecta logout → flag + alert urgent + OS notification → user navega a login (manual o futuro auto-navigate) → J-1 auto-fill (status flip optimista) → J-3 TOTP auto-inject si hay totpSecret → user clicks submit → J-2 proposeAutoSave (status:'active' en update path) → cookies set → anti-logout's watcher confirma login OK. **Tests** `tests/totp.smoketest.js` (~110 LOC, **17 asserts**): RFC 4648 base32 vectors (5: empty, whitespace tolerance, hex match, hello match, invalid char throws) + RFC 6238 Appendix B test vectors (6 timestamps × 8-digit codes — el conformance suite canónico) + 6-digit Google Authenticator format (3) + zero-padding (1) + 30-sec window stability (2). Validation contra el spec exacto. Suite full 74+ archivos verde. Lint clean. check:loc max 499. **Version bumps**: package.json + manifest 1.1.6 → 1.3.0 (minor bump per versioning_policy — J auto-login es feature grande pre-aprobada por Jose). Tests acumulados ~3169 → **~3186** (+17). **Pendiente**: smoke visual REAL con app corriendo — verificar flow completo IG + 2FA. G-6 (Ghost proxies import) **diferido a v1.2.x** post Apple — cero valor para Jose mientras su Ghost proxy list esté vacío.

- [`2026-05-15`] [`H-2-wire + H-2-extras / v1.1.6`] **Cierra el set H-2 completo: proxy-manager modal integrated + pre-bulk backup + diagnostic export** (1 commit, **+29 tests**, cero deps nuevas). Tres mejoras pequeñas que rematan el Proxy Ops Dashboard. **H-2-wire**: botón `📊 Open Dashboard` agregado al toolbar del proxy-manager modal existing (proxy-manager.js). Click → `window.oz.proxyHealth.openDashboard()` (IPC ya existía desde H-2a) → si ok, close del modal para que el user vea el dashboard tab. Si falla (no live window) → showError inline. El proxy-manager modal queda como vista compacta de CRUD; el dashboard tab es donde viven bulk ops, leak tests, coherence overlay, Oxylabs builder, diagnostics. **H-2-extra (a) pre-bulk-destructive backup**: nuevo módulo `browser/proxy-bulk-backup.js` factory `buildProxyBulkBackup({proxyManager, userDataDir, now}) → {snapshot, list, pruneOldBackups}`. Antes de `bulkDeleteProxies` (siempre) y `bulkSetDisabled(ids, true)` (solo al deshabilitar), snapshot del proxy pool entero a `userData/proxy-bulk-backups/<isoTs>.json` con shape `{ts, reason, ids, proxies}`. Retención: últimos 20 snapshots, prune automático post-snapshot. Failures swallow+log — backup nunca bloquea la op. `proxy-actions-bulk.js` factory ahora acepta `bulkBackup` optional dep; cuando wireada, bulk delete/disable retornan `result.backup = {ok, path, count, ts, reason}`. IPC `oz:proxyBulkBackup:list` para inspeccionar. Restore manual por ahora (auto-restore deferred — merge logic con post-backup creates es delicada). **H-2-extra (b) export diagnostic bundle**: nuevo módulo `browser/proxy-diagnostic-export.js` con `buildDiagnosticBundle({proxyManager, proxyAssignment, identityManager, workspaceManager, alertManager, leakTestHandlers, appVersion, platform}) → {meta, proxies, assignments, identities, workspaces, alerts, leakTests}` (pure, no Electron). **Sanitización dura**: `proxy.username` + `proxy.password` → `<redacted>`; cookies + accountVault NUNCA tocados; leakTests overview-only (sin `srflxIps`/`dnsServers` full por privacy). IPC `oz:proxyHealth:exportDiagnostic` → buildBundle + Electron `dialog.showSaveDialog` → `fs.writeFileSync` → returns `{ok, path, bytes}` o `{ok:false, reason:'CANCELED'|'EXPORT_FAILED', message}`. UI: nuevo botón `📋 Export diag` en dashboard header (después de `Test all now`). Wire via `browser/ui/proxy-dashboard-export.js` (~45 LOC IIFE exporta `window.OZ_DashboardExport.wire(btn, t)`) — extraído del proxy-dashboard.js para LOC budget. **Preload** `window.oz.proxyHealth.exportDiagnostic()` + `window.oz.proxyBulkBackup.list()`. **i18n**: 4 keys nuevas `proxyDashboard.{exportDiag, exportDiagUnavailable, exportDiagOk, exportDiagFailed}` EN + ES. **Version bumps** package + manifest 1.1.5 → 1.1.6. **Tests** 2 archivos nuevos: `tests/proxy-bulk-backup.smoketest.js` (~150 LOC, 9 asserts: factory guards + snapshot happy path + defensive errors + list + pruneOldBackups respeta MAX_KEPT=20). `tests/proxy-diagnostic-export.smoketest.js` (~180 LOC, 20 asserts: meta shape + sanitization es DURA — JSON.stringify NO contiene raw password ni raw customer username, redacted markers presentes, host/country/city/lastTestedIp NO redactados — + subsystems aggregated correctamente + leakTests sin srflxIps). Tests acumulados ~3140 → **~3169** (+29). Lint clean, `check:loc` max 499. **🎉 Cierra el set H-2 completo** (a/b/c/d/e/f/g/h/i/j/k/wire/extras) — el Proxy Ops Dashboard llega a feature-complete v1. **Próximo**: `1.2.0` G-6 Ghost importer también importa proxies (~3h), después `1.3.0` J auto-login (~6h). Smoke visual REAL pendiente con app corriendo.

- [`2026-05-15`] [`H-2k / v1.1.5`] **Oxylabs Proxy Builder modal** (1 commit, **+40 tests**, cero deps nuevas). Botón "+ Oxylabs" en el header del Proxy Health Dashboard abre un modal lazy-injected con form completo para generar batches de proxies Oxylabs Residential sin tener que armar manualmente los usernames `customer-X-cc-Y-city-Z-sessid-N-sesstime-M`. Killer feature del set H-2 — ahorra ~80% del setup tedioso. **Backend** `browser/proxy-providers.js` `expandOxylabs` extendido con `city` (slugified lowercase + underscore: "New York" → `city-new_york`) y `sticky:boolean` (default true; cuando false, omite sessid+sesstime para rotación residential — fresh exit IP per request). Cuando sticky=false todos los N items comparten el mismo username (la rotación la hace el upstream Oxylabs), y el `name` usa "rot N" suffix en vez de "#sessid". `PROVIDERS.oxylabs.fields` agrega el field city. Backward-compatible: defaults preservan el comportamiento previo (sticky on, sin city). **UI** `browser/ui/oxylabs-builder.js` (~310 LOC, lazy-injected modal con CSS inyectado on-demand) expone `window.OZ_OxylabsBuilder.{open, close, previewGenerate, COUNTRIES}`. Form fields: endpoint default `us-pr.oxylabs.io:10001`, customer, password, country dropdown (30 ISO codes priorizando LATAM — AR/BR/MX/CO/CL/PE first, luego US/ES/EU/Asia/Oceanía), city text, sticky checkbox (toggle hides/shows sesstime), sesstime select 10/30/60/120 min, startSessId, count 1-1000. **Preview live** primeros 5 generados se renderean en una tabla con sessId + username + host:port (font mono, word-break) — mirror client-side de expandOxylabs para evitar IPC roundtrip durante typing. **Validation inline**: endpoint debe ser host:port regex, customer + password requeridos, count 1-1000. Insert button disabled hasta validation green. Click "Insert proxies" → `window.oz.proxies.expandProvider('oxylabs', opts)` → `pm.bulkAdd` → broadcast → dashboard auto-refresh. **HTML** + `+ Oxylabs` button entre `+ Import` y `Bulk assign` en dashboard header. **proxy-dashboard.js**: btn-oxylabs click handler abre `OZ_OxylabsBuilder.open({t, refreshDashboard})`. **i18n** namespace nuevo `oxylabsBuilder.*` (13 keys: openBtn, title, endpoint, customer, password, country, city, sticky, sesstime, startSessId, count, preview, insert) EN + ES. **Tests** `tests/proxy-providers.smoketest.js` (~230 LOC, **34 asserts**, nuevo): validation guards (8) + happy path (6) + country (4) + city slugify (5) + sticky toggle (3) + startSessId/sesstimeMin (3) + listProviders/expandProvider dispatch (5). `tests/oxylabs-builder.smoketest.js` (~205 LOC, **6 asserts**): exports + COUNTRIES shape + previewGenerate null-state guard (open()+full preview validados en smoke visual end-to-end por dependencia de jsdom). **Version bumps**: package.json 1.1.4 → 1.1.5, manifest WebUI 1.1.4 → 1.1.5. **check:loc** max 500 (proxy-dashboard.js touched 500 LOC budget exactly tras agregar btn-oxylabs click handler). Lint clean. Tests acumulados ~3100 → **~3140** (+40). **Pendiente para 1.1.5.x**: H-2-wire (refactor proxy-manager existing modal para integrar dashboard) + H-2 extras opt-in (backup pre-bulk-destructivo + export diagnostic). Smoke visual REAL pendiente con app corriendo.

- [`2026-05-15`] [`H-2i+j / v1.1.4`] **Proxy Dashboard anti-detect coherence overlay + WebRTC/DNS leak tests** (1 commit, **+64 tests**, cero deps nuevas). El dashboard pasa de "proxy reachability" a "anti-detect coherence" — el status pill per-identity ahora deriva del peor de los 4 vectores de salud (`evaluateHealth` de E2-C-6) en lugar del legacy `leakRisk:boolean`, tooltip surface el worst-vector summary, botón inline **🔧 Apply geo** dispara `fingerprintEngine.applyGeoSuggestion()` para identidades con `ipTimezone` yellow/red (TZ del fingerprint no coincide con el country del proxy). Botón **🛡️ Leak test** por identidad spawnea hidden BrowserWindow con la session `persist:identity-<id>` (hereda proxy+cookies de los tabs reales del user), gather RTCIceCandidates 4.5s contra `stun:stun.l.google.com:19302`, hits `https://ipleak.net/json/` via `net.request` en paralelo, juzga ambos y muestra dialog con verdict + srflx IPs detectados + IP/country exit detectado. **H-2i (UI integration sobre backend existente)** — el backend de coherence (`anti-detect-health.js` + handlers con `FIX_KINDS.APPLY_GEO` ya wireado a `oz:health:applyFix`) viene de E2-C-6 y ya estaba completo + testeado; lo nuevo es `browser/ui/proxy-dashboard-health.js` (~135 LOC, IIFE expone `window.OZ_DashboardHealth`) con `fetchHealthMap` (Map<identityId, healthRecord>), `deriveStatus` (combina leakRisk + overall — leak risk wins porque no-proxy es peor que cualquier coherence mismatch), `buildStatusSummary` (worst-vector summary para tooltip — solo surface yellow/red), `renderFixButton` (solo APPLY_GEO inline; los demás FIX_KINDS quedan en health-modal para no clutear el row), `subscribeChanged` a `oz:health:changed` broadcast. Wirea en `proxy-dashboard.js` con state `healthMap` paralelo, fetch en `start()` + `setInterval 30s` + `btn-refresh`, applyFix from anywhere (sidebar, health-modal, MCP, inline button) re-fetches + re-renders sin polling adicional. **H-2j (greenfield)** — `browser/leak-tests.js` (~310 LOC, **pure** sin Electron) con `analyzeWebRtcCandidates` (mismatch srflx vs lastTestedIp → red WEBRTC_LEAK / public-IP host candidates → red WEBRTC_PRIVATE_LEAK / sin srflx pero matching → green / sin proxyPublicIp conocido → yellow inconclusive / candidates vacíos → yellow NO_STUN_RESPONSE), `analyzeDnsLeak` (IP_MISMATCH/COUNTRY_MISMATCH → red, DNS_GEO_MISMATCH soft → yellow), `combineLeakResults` worst-of, `parseRtcCandidate` (acepta structured object OR SDP candidate-string), `isPrivateRange` cubre RFC 1918 + IPv6 unique-local + link-local, `isMdnsLocal` para los `.local`. `browser/leak-tests-handlers.js` (~290 LOC) factory `buildLeakTestHandlers(browser) → {run, get, list, clear}` con cache in-memory `browser._leakTestCache`. `run({identityId})` ejecuta WebRTC + DNS en `Promise.all`, gracefully cae a yellow si cualquiera falla, broadcast `oz:leakTest:changed`. **WebRTC runtime**: `BrowserWindow({show:false, webPreferences:{partition: 'persist:identity-<id>', contextIsolation:true, sandbox:true, backgroundThrottling:false}})` + `loadURL(data:text/html;base64,...)` con script inline que registra `pc.onicecandidate` a `window.OZ_CANDIDATES`, polls cada 300ms via `executeJavaScript`, early-exit cuando hay srflx >1.5s elapsed, hard timeout 4.5s, `win.close()` en finally. **DNS runtime**: `session.fromPartition('persist:identity-<id>')` → `net.request('https://ipleak.net/json/')` timeout 6s. **IPC** 4 channels `oz:leakTest:{run,get,list,clear}` + broadcast `oz:leakTest:changed`. **Preload** `window.oz.leakTest.{run, get, list, clear, onChanged}`. **UI** `browser/ui/proxy-dashboard-leaks.js` (~135 LOC) con `fetchLeakMap` + `renderLeakButton` (botón + last-result pill badge) + `runLeakTest` + `formatResultDialog` (multi-line para `window.alert` — modal full deferred). **proxy-dashboard-actions.js** cases `'apply-geo-fix'` + `'run-leak-test'`. **i18n** namespaces nuevos `proxyDashboard.{coherence, leak}.*` (17 keys × 2 idiomas). **Version bumps** package 1.1.3 → 1.1.4, manifest WebUI 1.1.3 → 1.1.4. **Tests** 3 archivos +64 asserts: `leak-tests.smoketest.js` (34: parseRtc 6 + helpers 8 + analyzeWebRtc 6 + analyzeDns 7 + combine 5), `proxy-dashboard-health.smoketest.js` (18: vm-evaluated IIFE + deriveStatus/buildSummary/renderFix), `proxy-dashboard-leaks.smoketest.js` (12: render+summary+dialog). Suite full 70 archivos verde. Lint clean. Wire-up parity 9/9 IPC channels preload↔backend. Tests acumulados ~3036 → **~3100**. **Pendiente v1.1.5**: H-2k Oxylabs Builder + H-2-wire + H-2 extras. Smoke visual REAL pendiente con app corriendo (regla feedback_smoke_visual_bugs).

- [`2026-05-15`] [`H-2h / v1.1.3`] **Proxy Dashboard bulk assign 1:1** (1 commit, **+51 tests**, cero deps nuevas). Modal "Bulk assign proxies (1:1)" desde botón "Bulk assign" en dashboard header (también accesible via "Reassign…" del bulk action bar H-2f cuando hay identities seleccionadas). SOLO 1:1 mapping (decisión Jose 2026-05-15 — NO round-robin, NO random). Si N proxies ≠ M identities, warning visible + las primeras min(N,M) se aparean en orden, el resto sobra. Source selector (proxies, multi-select con search + checkbox) + target selector (identities, multi-select con search + checkbox) side-by-side. Filtros automáticos: proxies excluyen `isDisabled`, identities excluyen `isDefault` (no aplica proxy). Preview area se actualiza on-change con tabla "Proxy → Identity" + warning rojo soft si N!=M con counts interpolados. **Backend** `browser/proxy-bulk-assign.js` (~120 LOC) factory `buildProxyBulkAssign({proxyManager, proxyAssignment, proxyActions}) → {previewPairing, executePairing}`. `previewPairing(proxyIds[], identityIds[], opts?) → {pairings: [{proxyId, identityId, proxyName, identityName}], leftoverProxies, leftoverIdentities, warning|null, counts: {proxies, identities, paired}}`. opts.identityNamesById permite que la UI suministre nombres sin que el backend tenga que cruzar IdentityManager. `executePairing(pairings[])` itera secuencial: assignToIdentity primero (source of truth), después best-effort proxyActions.reloadSession (cascade UX). Si reloadSession falla (return ok:false o throw `THREW`), el assignment sigue ok:true — el reload es UX, no consistencia. Bad pairs (`{proxyId: null}` o sin identityId) flagged como `BAD_PAIR` sin call. Top-level `ok` es `failed === 0` para que la UI sepa si hubo problemas en cualquier pair. **IPC** 2 channels nuevos `oz:proxyBulkAssign:{preview, execute}`. **Preload** `window.oz.proxyBulkAssign.{preview, execute}`. **UI** `browser/ui/proxy-dashboard-bulk-assign.js` (~230 LOC, lazy-injected modal). Selection state interno `_sel.{proxyIds: Set, identityIds: Set, snapshot}` — NO comparte state con H-2f bulk module (porque H-2f opera sobre row checkboxes del dashboard mientras H-2h tiene su propio modal con su propio scope). Botón "Assign N pairs" disabled cuando paired === 0. window.alert post-execute con summary. **Cross-feature wire** con H-2f: el bulk action bar de identities tiene "Reassign…" que llama `OZ_BulkAssign.openForIdentities(ids)` pre-seleccionando las identities del Set H-2f — gracefully fallback a window.prompt si el módulo H-2h no está cargado. **i18n** `proxyDashboard.bulkAssign.*` ya estaba en placeholders desde H-2f commit, re-utilizadas las 10 keys EN + ES. **Tests** `tests/proxy-bulk-assign.smoketest.js` (~230 LOC, **51 asserts**): factory guards, previewPairing N===M happy + N<M warning + leftoverIdentities + N>M warning + leftoverProxies + N=0/M=0/both=0 defensive + null/undefined inputs; executePairing happy 3-pair + assignToIdentity throws → ok:false con reason + reloadSession fails → assignment still ok + reloadSession throws → captured + bad pair `{proxyId: null}` skipped. **Cierra el set H-2 (Proxy Ops Dashboard completo): H-2a badge + H-2b dashboard + H-2c per-proxy + H-2d per-identity + H-2e diagnostics/alerts + H-2f bulk multi-select + H-2g bulk import + H-2h bulk assign**. Tests acumulados ~2985 → **~3036** (+51). Lint clean. check:loc max 498.

- [`2026-05-15`] [`H-2g / v1.1.3`] **Proxy Dashboard bulk import CSV/TXT con auto-detect** (1 commit, **+71 tests**, cero deps nuevas — `csv-parse@^6.2.1` ya estaba para el flow 1.8d). Modal "Import proxies" accesible desde botón "+ Import" en dashboard header. User pega texto o uploadea archivo .csv/.txt/.tsv (FileReader local, NO toca disco main process). Parser auto-detecta entre 3 formatos: **TXT host:port:user:pass** (Oxylabs / SmartProxy default — 4 colon-separated parts, también soporta 2-field sin creds y 5-field con country), **TXT user:pass@host:port** (URL-style sin scheme, regex `^([^:@\s]+):([^@\s]+)@([^:@\s]+):(\d+)$`), **CSV con headers** (host,port,user,pass + opcional country,label,type/protocol — case-insensitive aliases User/USER/Username/etc). Heurística detect: csv si first line tiene "host" + "port" keywords y coma; url-style si cualquier línea matchea el regex; host-port si línea matchea 4-parts colon o 2-parts host:port; default unknown. **Backend** `browser/proxy-importer.js` (~210 LOC) factory `buildProxyImporter({proxyManager}) → {detectFormat, parseProxies, parseTxtHostPort, parseTxtUrlStyle, parseCsvAll, importBatch}`. Cada parsed row tiene `row: lineNumber` 1-indexed (CSV rows comienzan en row 2 — header en row 1) para que la UI marque errores con precisión. parseProxies retorna `{format, rows: [{row, ok, proxy?, reason?, message?}], summary: {total, valid, invalid}}`. Skipea líneas con `#` (comentarios) y blank lines pero preserva los row numbers reales. importBatch llama `proxyManager.create()` por cada valid (NO bulkAdd — queremos fail-fast por row con reason propia), retorna `{ok, added, failed: [{row, reason}], addedIds, summary}`. **IPC** 3 channels nuevos `oz:proxyImporter:{detect, parse, import}`. **Preload** namespace `window.oz.proxyImporter.{detect, parse, import}`. **UI** `browser/ui/proxy-dashboard-import.js` (~270 LOC, lazy-injected modal en first open). Modal con header + textarea grande con placeholder mostrando los 3 formatos ejemplo + file input accept=".csv,.txt,.tsv" + checkbox "Re-parse on text change" (live debounce-less — solo dispara on input event) + detected-format pill (accent color para CSV/URL/host-port, red para unknown) + Parse button manual + preview table (cap 50 rows: row#, status pill green/red, host, port, user, country, error inline) con sticky thead + summary "N rows · M valid · K invalid". Botón "Import N valid proxies" disabled hasta que haya >0 valid. window.alert al finalizar con count agregado + refreshDashboard. **CSS** inline en el módulo (no toca proxy-dashboard.html CSS) — backdrop fixed 0.55α + modal max 900px width 90vh height + sticky preview thead. **i18n** `proxyDashboard.import.*` ya estaba en placeholders del H-2f commit anterior — re-utilizadas las 16 keys EN + ES. **Tests** `tests/proxy-importer.smoketest.js` (~220 LOC, **71 asserts**): detectFormat happy/edge/garbage los 3 + unknown, parseTxtHostPort happy + port 0/99999/non-numeric/1-field/empty-host invalid, parseTxtUrlStyle happy + missing-pass + no-@ + double-@ invalid, parseCsvAll headers + extra cols + invalid host/port rows, parseProxies dispatcher con `#` comments preservando line numbers, importBatch happy + proxyManager error code propagation + addedIds tracking. Lint clean. check:loc max 498. Tests acumulados ~2914 → **~2985** (+71).

- [`2026-05-15`] [`H-2f / v1.1.3`] **Proxy Dashboard bulk multi-select + bulk actions** (1 commit, **+37 tests**, cero deps nuevas). Checkbox column en ambas tablas del dashboard (Identities + Proxies) con header select-all-visible tri-state (checked / indeterminate / unchecked), selection state cross-pagination + cross-filter via `Set<id>` (re-render mantiene los checkboxes marcados por id, no por row index), action bar contextual sticky entre toolbar y tabla que aparece cuando hay >0 seleccionados. **Backend** `browser/proxy-actions-bulk.js` (~95 LOC) factory `buildProxyActionsBulk({proxyActions}) → {bulkTestProxies, bulkResetProxies, bulkSetDisabled, bulkDeleteProxies, bulkReloadSessions}`. Cada wrapper itera secuencial (NO Promise.all — evita saturar el daemon health + upstream proxy + logs ordenados) y retorna `{ok: true, results: [{id, ok, ...detail}], summary: {total, ok, failed}}`. `ok:true` a top-level = "ejecutamos el ciclo", no que todos hayan funcionado — los items individuales tienen su propio `ok`. Throws en single calls capturados como `{ok:false, reason:'THREW', message}`. **IPC** 5 channels nuevos `oz:proxyActionBulk:{test,reset,setDisabled,delete,reloadSessions}` con lazy builder reutilizando el `actions()` builder existente de H-2c/d. **Preload** namespace nuevo `window.oz.proxyActionBulk.{test, reset, setDisabled, delete, reloadSessions}` en `preload-proxy.js`. **UI** delegado a `browser/ui/proxy-dashboard-bulk.js` (~150 LOC, mismo patrón que proxy-dashboard-alerts.js — IIFE expone `window.OZ_DashboardBulk`). State module-level con `identSelected: Set<id>` y `proxySelected: Set<id>`. Helpers `rowCheckboxHtml(kind, id)` (genera checkbox con state pre-marcado), `setVisibleIds(kind, ids)` (que el host llama post-paginación), `syncActionBar(kind, t)` (actualiza contador + header tri-state checkbox + visibility), `handleDelegatedClick(ev, deps)` (resuelve toggle-row / select-all / cada bulk action). Bulk action bar identities: "↻ Reload sessions" + "Reassign…" (delega a H-2h modal si está cargado, fallback prompt 1 proxy → N identities). Bulk action bar proxies: Test (primary) + Reset + Disable + Enable + Delete (danger + window.confirm con `{{n}}`). **proxy-dashboard.js** queda en 415 LOC: el `performAction` single-row se extrajo a `browser/ui/proxy-dashboard-actions.js` (~90 LOC) para hacerle lugar al wireup del bulk module. **CSS nuevo** `.bulk-actionbar` sticky con border accent + `.bulk-cb-col` para columns checkbox + accent-color del native checkbox alineado al theme. Botones nuevos en dashboard header: "+ Import" (placeholder hasta H-2g) y "Bulk assign" (hasta H-2h). **i18n** namespaces nuevos `proxyDashboard.{bulk,import,bulkAssign}.*` (~35 keys EN + ES — incluye los placeholders de H-2g/h para evitar re-edit en próximos sub-bloques). **Tests** `tests/proxy-actions-bulk.smoketest.js` (~210 LOC, 37 asserts): 5 wrappers × happy/fail-mixed/throws/empty/null/undefined/idempotent. Lint clean. check:loc max 498. Tests acumulados ~2877 → **~2914** (+37).

- [`2026-05-15`] [`H-2e / v1.1.3`] **Proxy Diagnostics + Alerts engine** (1 commit, **+28 tests**, cero deps nuevas). Engine pasivo que escanea el subsystem proxies cada 5 min y dispara alertas automáticas cuando detecta condiciones accionables. **`browser/proxy-diagnostics.js`** (~250 LOC) factory `buildProxyDiagnostics({proxyManager, proxyAssignment, identityManager, alertManager, notify, logger, now, dedupUrgentMs, dedupWarningMs}) → {scan, getAlerts, dismissAlert, dismissAll, _internalState}`. Compute-only — no muta state, solo lee. **4 triggers**: (1) `proxy-disabled` severity=urgent (cualquier `p.isDisabled=true`), (2) `identity-unassigned` severity=urgent (THE bug Contexto IG — non-default identity con `proxyAssignment.resolve()=null`), (3) `proxy-stale` severity=warning (lastTestedAt>24h o never tested, skipped si ya disabled), (4) `latency-spike` severity=warning (2 lecturas consecutivas >2000ms — rolling 2-point history per proxy via lastTestedAt freshness key). **Dedup**: ventana 6h urgent / 24h warning per `(kind,targetId)`. dismissAlert resetea el dedup (re-firea al instante si vuelve a ocurrir). Alerts forward a `alertManager.add()` (panel) + callback `notify(title,body,severity)` (OS notification). Todos los failures de alertManager.add y notify swallow-and-log — engine nunca rompe scan. **Action routing en alertManager.add**: `identity-unassigned` → `{kind:'open-dashboard'}`, demás → `{kind:'open-modal', payload:{modal:'proxyManager'}}`. **IPC nuevos** 4 channels: `oz:proxyDiagnostics:{scan,getAlerts,dismissAlert,dismissAll}` con singleton `browser._proxyDiagnostics` build-once en `registerProxyHealthGlobalHandlersIPC`. **Preload** namespace nuevo `window.oz.proxyDiagnostics.{scan,getAlerts,dismissAlert,dismissAll}` en `preload-proxy.js`. **main.js**: setInterval 5 min post-`proxyHealth.startDaemon` que llama `_proxyDiagnostics.scan()` — wirea OS notifications independientes de si el dashboard está abierto. Cleanup en before-quit. **Dashboard UI** nueva section "Alerts" arriba de Identities (card consistent con resto del dashboard). Cada alert renderizado con icon ⚠/ⓘ según severity, title bold + message muted + meta `URGENT · 5m ago` + button "Dismiss" per row + "Dismiss all" en toolbar. CSS data-severity colors (urgent=red tint border 0.5α, warning=yellow tint 0.45α). Auto-fetch+render alerts cada 30s mientras tab visible, en paralelo con dashboard data. **i18n** catalog nuevo namespace `proxyDashboard.alerts.*` (8 keys EN + ES). **Version bumps**: package.json 1.1.2 → 1.1.3, manifest WebUI 1.1.2 → 1.1.3. Lint clean. Tests acumulados ~2849 → **2877** (+28). **Pendiente del H-2 completo (1.1.3)**: H-2f bulk multi-select + bulk actions, H-2g bulk import CSV/TXT con auto-detect, H-2h bulk assign 1:1 mapping.

- 🎉 **BLOQUE E2-C COMPLETO CERRADO** (C-1 + C-2 + C-3 + C-4 + C-5 + C-6 + C-7 + C-8 + sidebar bidirectional sync + tooltip fix). Validación visual end-to-end pendiente para C-6/C-7/C-8. Próximo chunk: Bloque D Backup + Sync Dropbox (~13-15h). Etapa 3 Apple signing bloqueada ~2d por approval.

- [`2026-05-14`] [`v1.0.0`] **Version bump 0.1.0 → 1.0.0** (decisión Jose). Policy: v1 line = 1.0.x (patch bump per release), v2 line = 2.0.x, v3 line = 3.0.x. Cambios: `package.json` 0.1.0→1.0.0; nuevo IPC `oz:app:getVersion` + preload `window.oz.app.getVersion()`; About panel ahora muestra "Version: 1.0.0" + "Settings schema: 1" (antes solo el schema disfrazado como Version); manifest WebUI 1.0.5→1.0.6 (regla bump per UI edit).

- [`2026-05-15`] [`H-2c+d / v1.1.2`] **Proxy Dashboard live ops: actions per-proxy + per-identity** (1 commit, +0 tests pero sanity-tested). El dashboard pasa de read-only a operacional. **`browser/proxy-actions.js`** (~190 LOC) factory `buildProxyActions({proxyManager, proxyAssignment, proxyHealth, identityManager, toProxyRulesString}) → {testProxy, resetProxy, setDisabled, rotateSticky, deleteProxy, reloadSession, reassignProxy}`. Per-proxy: **test** vía proxyHealth.testOne, **reset** (failureCount=0, isDisabled=false, lastTestedAt=null, re-test best-effort), **setDisabled** toggle, **rotateSticky** (regex `-sessid-XXX-` en username → nuevo random base36 — sanity-tested 3 escenarios incluyendo null + sin-sessid → null correcto), **deleteProxy** (clearByProxyId en assignments + proxyManager.remove). Per-identity: **reloadSession** (re-aplica proxy actual sobre la session cacheada vía `session.setProxy({proxyRules})` — **THE fix para el bug de Contexto IG** donde la session se creó pre-1.0.0 sin proxy y quedó con direct:// cacheado), **reassignProxy** (assignToIdentity + cascade reloadSession). Todas las funciones retornan `{ok, ...}` y NUNCA throw — errores → `ok:false + reason + message`. **IPC** 7 nuevos `oz:proxyAction:{test,reset,setDisabled,rotateSticky,delete,reloadSession,reassign}` con lazy actions builder. **Preload** extracted to `preload-proxy.js` (~30 LOC) — preload.js pasaba 500 LOC con las 2 namespaces nuevas, ADR 0005 obligaba split. Spread `...require('./preload-proxy').buildProxyBindings(ipcRenderer)` mantiene `window.oz.proxyHealth.*` + `window.oz.proxyAction.*` sin cambios para consumers. **Dashboard UI** nuevas columnas "Actions" en ambas tablas. Identidades: botón primary **Reload session** (resuelve el bug Contexto IG en 1 click) + dropdown **Reassign proxy** con (None / auto-random / auto-round-robin / lista completa de proxies con country). Identity 'default' muestra "default — n/a" (no se opera). Proxies: botones **Test** (primary), **Reset**, **Disable/Enable** (toggle), **Rotate** (solo si protocol≠socks5, hides el botón cuando no hay -sessid- marker via NOT_STICKY error → alert al user), **Delete** (danger + confirm dialog). Delegated event handlers para click + change (selects) sobre body — escala con paginación + futuros agregados. confirmDialog usando window.confirm/alert (suficiente para v1.1.2, modal full llega en v1.1.3 con Edit). CSS nuevo `.row-actions` con flex-wrap + variant `.primary` / `.danger`. **i18n** catalog `proxyDashboard.actions.*` (16 keys EN + ES). **Footer note** updated: "Live ops view. Actions apply immediately. Edit modal arrives in v1.1.3." **Version bumps**: package 1.1.1 → 1.1.2, manifest WebUI 1.1.1 → 1.1.2. Lint clean. check:loc max 495.

- [`2026-05-15`] [`H-2b / v1.1.1`] **Proxy Health Dashboard tab read-only** (1 commit, +0 tests pero sanity-checked). Página dedicada full-screen abierta vía new tab (`chrome-extension://<webui-id>/proxy-dashboard.html`). Click en badge global (H-2a) ahora abre el dashboard en lugar del modal proxy-manager. **`browser/proxy-dashboard-data.js`** (~95 LOC) aggregator: cruza ProxyManager + ProxyAssignment + IdentityManager + WorkspaceManager → snapshot `{globalStatus, identities:[], proxies:[], capturedAt}`. Per-identity rows incluyen workspace/proxy resolved/leakRisk flag (true cuando non-default identity sin proxy). Per-proxy rows incluyen usedByCount + usedBy preview (cap 20) + status (red disabled / yellow failures-stale-untested / green). **IPC** nuevos `oz:proxyHealth:getDashboard` (snapshot) + `oz:proxyHealth:openDashboard` (abre tab usando `browser.windows[*].tabs.create(...)` con la URL del WebUI extension; selecciona el sender window primero, fallback al primer live window). **Preload** `window.oz.proxyHealth.{getDashboard, openDashboard}`. **`browser/ui/proxy-health-badge.js`** ya soportaba el path opcional `openDashboard` desde H-2a — ahora ese path se activa porque la binding existe. **`browser/ui/proxy-dashboard.html`** página standalone con hero status (dot grande + h1 + hint + Refresh/Test-all buttons), section Identities (table con identity/workspace/proxy/country/status + search + paginación 25/page + sort-by-column), section Proxies (table con name/host/country/used-by/status/last-test/latency + idem search/page/sort). CSS oscuro inline en `<style>` para no depender del CSS del webui. Auto-refresh 30s mientras tab visible + visibility-pause. Read-only en 1.1.1 — todas las acciones (test/reset/edit/reassign/etc) llegan en H-2c (1.1.2). i18n-ready: catalog namespace `proxyDashboard.*` con ~22 keys EN + ES. **`browser/ui/proxy-dashboard.js`** (~250 LOC) controller IIFE con state interno (search/sortKey/sortDir/page por tabla), renderers per-section, fetch via preload. Sanity test inline 3 escenarios OK: empty pool / healthy / leakRisk identity. Lint clean. check:loc max 495.

- [`2026-05-14`] [`H-2a / v1.1.1`] **Proxy Health Badge global (primera entrega de H-2 Proxy Ops Dashboard)** (1 commit, +0 tests, 0 deps nuevas). Indicador dot rojo/amarillo/verde/gris en toolbar superior (entre URL bar y browser-action-list). Status agregado calculado en backend desde ProxyManager + ProxyAssignment + IdentityManager. Decision tree: cualquier proxy disabled OR identity sin proxy assigned → red (leak risk); cualquier failure/untested/stale (>24h) → yellow; all-clear con al menos 1 proxy → green; pool vacío → gray. **`browser/proxy-health-status.js`** (~110 LOC) módulo puro `computeGlobalStatus({proxyManager, proxyAssignment, identityManager}) → {status, counts, lastTestedAt, hint}`. Counters: total/ok/fail/disabled/untested/stale/unassigned/identities/identitiesWithProxy. Sanity-tested inline contra 5 escenarios (empty/healthy/disabled/untested/mixed) — todos correctos. **IPC** nuevos `oz:proxyHealth:getGlobalStatus` (read) + `oz:proxyHealth:testAllAndStatus` (force-scan via daemon.testAll y retornar status fresco) en `ipc-handlers-extra.js`, registrados al inicio de `registerExtraIpcHandlers`. **Preload** `window.oz.proxyHealth.{getGlobalStatus, testAllAndStatus}`. **UI** botón nuevo `#oz-proxy-health-badge` en `<div class="toolbar">` después de address-bar, CSS pulse animation amarillo/rojo cuando hay problema, transición smooth de color. **`browser/ui/proxy-health-badge.js`** (~95 LOC) IIFE `window.OZ.proxyHealthBadge` con poll cada 30s + visibility-aware (pausa al ocultar tab). Click → por ahora abre el modal proxy-manager existente (botón `#oz-pm-button`); en H-2b se redirecciona al dashboard tab. Tooltip i18n-aware con fallback al `hint` del backend. **Catálogos i18n** extendidos con namespace `proxyHealth.badge.tooltip{Green,Yellow,Red,Gray}` en EN + ES. **Version bumps**: package 1.1.0 → 1.1.1, manifest WebUI 1.1.0 → 1.1.1. Lint clean. check:loc max 495. **Roadmap v1 completo** documentado en memoria `project_v1_roadmap.md` (decisión Jose 2026-05-14 — H-2 a-k + G-6 + J auto-login + bulk-open + session warmer + identity HUD + onboarding wizard + Mac sleep + i18n full + smoke visuals + I + I-2, ~63-66h total v1 pendiente).

- [`2026-05-14`] [`i18n v1 / v1.1.0`] **Internacionalización UI EN/ES — primera entrega** (1 commit, +0 tests, 0 deps nuevas, ~150 LOC infra + 2 catálogos JSON). Liviano custom (sin i18next). Decisión Jose 2026-05-14 — LATAM neutral, glossary acordado (Workspace→Espacio, Identity→Identidad, Vault→Bóveda, Settings→Configuración, Backup→Respaldo, etc). **`browser/ui/i18n.js`** (~190 LOC) clase `I18n` con `init()` async, `t(key, params)` con `{{var}}` interpolation + fallback a EN + fallback al key, `setLocale('auto'|'en'|'es')` persistente, `translatePage()` que pasa por `data-i18n` (textContent) y `data-i18n-attr="placeholder:key1,title:key2"` (attrs), `onChange()` listener. Auto-init en DOMContentLoaded, idempotent. Expone `window.OZ.i18n` + `window.OZ.t` shortcut. **Catálogos** `browser/ui/locales/{en,es}.json` con ~70 strings cubriendo: common (Save/Cancel/Loading/etc), sidebar headers + New Workspace, Settings nav (10 tabs), Settings General (incl Language dropdown), Settings About, Settings Migration full (intro + radios + summary + replaceConfirm + interpolated strings via `{{identities}}/{{workspaces}}`), language dropdown labels. **Boot flow**: settings.json `general.locale: 'auto'|'en'|'es'` (default `auto`). Auto → IPC `oz:app:getSystemLocale` → `app.getLocale()` → normalize 2-char prefix → load matching catalog. Else load forced locale. translatePage on first render. **UI changes**: nueva fila "Language" en Settings → General con `<select>` (Auto/English/Español). Bind via `settings.js` con `localeSpecial: true` que delega a `window.OZ.i18n.setLocale()` (NO al save genérico — i18n module ya persiste). Tags `data-i18n` en Settings nav, General section, About panel (incl Version/Settings schema/subtitle/description/Reset all button), Migration section completa (h3 + intro + ghost install row + last import row + checkboxes + mode radios + import button + progress + Forget history button), sidebar headers (Workspaces, WORKSPACES & IDENTITIES, + New Workspace, Show archived). **`browser/migrations/ghost-browser-importer.js`** sin cambios (ya estaba en EN canónico). **`browser/ui/ghost-migration-ui.js`** usa `window.OZ.t()` para `replaceConfirm` + las strings interpoladas `lastImportSummary` y `prevCount` con `{{identities}}/{{cookies}}/{{passwords}}/{{workspaces}}`. Fallback inline si i18n no cargó. **`browser/ipc-handlers-extra.js`** + **`preload.js`**: new IPC `oz:app:getSystemLocale` + preload binding `window.oz.app.getSystemLocale()`. **`browser/settings-manager.js`** schema extendido `general.locale` + validateKey enforce `auto|en|es`. **Cobertura parcial v1.1.0** (~30% del UI): Settings panel completo (most-used) + Migration (recién shippeada) + sidebar. **Pending v1.1.x patches**: modales (health, identity-clone, account-manager, time-machine, cloud-backup, team, proxy-manager, extensions-modal), notifications.js toast messages, onboarding flow, error messages dinámicos en handlers. Cualquier string sin key en catalog → graceful fallback al texto literal EN (DOM no se rompe). **Version bumps**: package.json 1.0.1 → **1.1.0** (minor — feature grande dentro de v1, confirmado por Jose), manifest WebUI 1.0.7 → 1.1.0 (alineado). Lint clean (`gc` warning pre-existente no mío). check:loc max 495.

- [`2026-05-14`] [`G-5 / v1.0.1`] **Ghost importer G-5: idempotency + replace mode + self-heal** (1 commit, +38 tests). 3 bugs fixed surfaced por Jose's smoke 2026-05-14: **Bug A (idempotency)** — `runImport` ahora acepta `opts.mode = 'merge' | 'replace'` (default merge). Merge consulta `ghost-migration-state.json` pre-create y skipea ghost-ids ya mapeados; state.json se merge incremental, no se sobreescribe. Replace borra identities + workspaces del state previo antes de re-importar; state.json fresh. Counters nuevos en summary: `reused.{identities, workspaces}` y `removed.{identities, workspaces}`. **Bug B (sidebar wipe at boot)** + **Bug C (boot reconcile wipes identityIds)** — root cause: el importer pre-G-5 llamaba `workspaceManager.addIdentity(ws.id, ozId)` sin actualizar `identity.workspaceId`, y al boot `syncIdentityWorkspaces` rebuilds workspace.identityIds[] desde `identity.workspaceId='general'` (default) → workspace.identityIds vuelve a []. Fix: importer ahora llama `identityManager.moveToWorkspace(ozId, ws.id)` (que dispara el hook bidireccional y setea ambos lados). **Defense-in-depth self-heal:** `identity-workspace-sync.js` agrega Step 1.5 — para cada workspace non-general, scan `tabSpecs[].identityId`; si el identity referenciado tiene `workspaceId !== ws.id`, infer ownership y move identity al workspace (first-wins on conflict). Recupera Jose's userData en próximo boot del 1.0.1 sin necesidad de repair manual. **UI G-3**: radio buttons "Merge with existing (recommended)" / "Replace previous import (destructive)" visibles solo cuando state.json existe; confirm dialog antes de replace. Summary muestra `Mode: merge|replace` + reused/removed counts. **Tests nuevos** `tests/ghost-browser-importer-g5.smoketest.js` (38 assertions): merge idempotency (run twice → 2nd run reused=N created=0), replace cleanup (removed=N, survivor identity untouched), self-heal from tabSpecs (broken state → wireIdentityWorkspaceSync moves identity to correct workspace), General Browsing excluded del self-heal. **E2e test** (`ghost-browser-importer-e2e.smoketest.js`) actualizado: ahora wirea `wireIdentityWorkspaceSync` para mirror production. **Version bumps**: package.json 1.0.0 → 1.0.1, manifest WebUI 1.0.6 → 1.0.7. Lint clean. check:loc max 495.

## Histórico

- [`2026-05-13`] [`D-3c-3c`] **Sync wire-up final — main.js + Settings UI + IPC + MCP** (1 commit, ~3h efectivas, cero deps nuevas, +83 tests). Cierra el sub-chunk pendiente del Bloque D — el engine + queue + puller + apply-remote del D-3a→D-4 mini b ya están enchufados al ciclo de vida real. 3 decisiones vía AskUserQuestion (recomendadas): default opt-in en Settings (no auto-start sorpresivo), push-all inicial en primer enable (sin esto los devices no convergen), UI mínima toggle + status pill + Sync Now. **Módulos nuevos**: `browser/sync-bootstrap.js` (orchestrator entre setupSync y main.js — init/setEnabled/getStatus/pullNow/stop API, lazy build, cold-start counter en `settings.sync.firstEnableAt` para que reruns NO dupliquen el push-all, alert urgente cuando NEEDS_REAUTH 401 de Dropbox, broadcast `oz:sync:changed` para refresh UI live), `browser/sync-bootstrap-setup.js` (glue para main.js con setupSyncBootstrap/startSyncBootstrap/stopSyncBootstrap), `browser/sync-handlers.js` (handler map IPC+MCP shared con guards NOT_CONFIGURED + BAD_ARG), `browser/mcp-tools-sync.js` (3 tools `oz.sync.{getStatus,setEnabled,pullNow}`), `browser/mcp-server-setup.js` (split del MCP boot del main.js para LOC budget, mismo patrón crash-recovery-setup), `tests/_helpers-sync-bootstrap.js` (fakes compartidos). **main.js wire-up**: setupSyncBootstrap post-bookmarkManager + pre-registerIpcHandlers + startSyncBootstrap post-registerIpcHandlers (init() resume del engine si user opted-in y Dropbox sigue authenticated) + stopSyncBootstrap FIRST en before-quit (antes de vault.lock para evitar getMasterKey-on-locked-vault noise — la queue ya persiste per enqueue). **Reusa `browser.dropboxClient` de D-1 setupCloudBackup** — mismo Keychain `'dropbox'` provider, mismo OAuth callback `oz://auth/dropbox/callback` (no se duplica el flow). **Settings extension** v1 + `sync: {enabled:false, firstEnableAt:null}` con validateKey. **Preload** `window.oz.sync.{getStatus,setEnabled,pullNow,onChanged}` + 3 IPC channels. **Settings UI new section "Sync"**: toggle + status pill (5 estados: Running/Stopped/Dropbox not connected/Needs re-auth/Vault locked/Starting) + Sync Now (disabled si !running) + descripción de privacy ("encrypted locally with your Vault master key before upload — Dropbox cannot read them"). Modal subscribes a `onChanged` para refresh sin re-abrir. **Refactor incidental MCP boot** (ADR 0005, main cruzó 500 con mis additions): extraído a `mcp-server-setup.js`. main.js max 495 post-extract. **Diferido**: D-3c-3d long-poll real (sigue setInterval 30s), tombstone GC, panel dedicado con stats, Vault EventEmitter para resume auto on unlock. **Tests**: 18 run blocks en sync-bootstrap.smoketest (59 tests) + 3 en sync-bootstrap-handlers.smoketest (12 tests) = +71 propios. Tests acumulados ~2236 → ~2319 (+83). Lint clean. check:loc verde max 495. `docs/history/30-bloque-d3c3c-resultado.md`. **Validación visual end-to-end pendiente para Jose**: `npm start` con `OZ_DROPBOX_APP_KEY` + Dropbox conectado en Cloud Backup → Settings → Sync → toggle Enable → confirmar cold-start counts + queue drain + tras unos segundos `oz.sync.getStatus` reporta running:true. Test cross-device: repetir en otra Mac y verificar identities aparecen vía pull <60s.

- [`2026-05-13`] [`D-3a → D-4 mini b`] 🎉 **Sync engine — internal layer feature-complete** (8 commits `d2b947e` → `befc354`, **+460 tests**, todos CI verde). Toda la stack de sincronización Dropbox según ADR 0026 implementada y testeada, salvo wire-up final en main.js (requiere live validation). **Módulos nuevos** (~2500 LOC + tests): `browser/sync-merge.js` (LWW + tombstones + deviceFolder lex desempate, pure logic, 40 tests), `browser/sync-record-store.js` (AES-256-GCM encode/decode, mismo formato que D-1 backup, 39 tests), `browser/sync-queue.js` (FIFO persistente con coalesce, 63 tests), `browser/sync-engine.js` (push side: drain loop + backoff exponencial 1s→30s + race-safe conditional remove que jamás pierde edits durante upload, scheduler inyectable, 63 tests), `browser/sync-pull.js` (pull side: cursor en `userData/sync-state.json`, cold-start `listFolder` → subsequent `listFolderContinue`, decode + LWW merge + emite `'remote-apply'` events, 59 tests), `browser/identity-manager-sync.js` (applyRemoteUpsert/Delete standalone funcs que mutan IM sin emitir `'changed'` — corta el loop infinito remote→local→push→remote, 39 tests), `browser/workspace-manager-sync.js` (mismo pattern + privacy carveout: tabSpecs/activeTabId stripped en push Y apply, 38 tests), `browser/bookmark-manager-sync.js` (single-record full-file LWW recordId='all', 49 tests con verificación de sidecar persistente), `browser/sync-setup.js` (orchestrator: `setupSync({vault, dropbox, identityManager, workspaceManager?, bookmarkManager?, userDataDir, deviceFolder}) → {engine, puller, queue, start, stop, pullNow}`, wirea todo + poll loop 30s, end-to-end Alice→Bob round-trip testeado, 46 tests entre core+workspace). **Managers modificados** ahora extienden EventEmitter y emiten `'changed'` tras cada CRUD efectivo con payload `{op, recordType, recordId, record?, updatedAt|deletedAt}`: IdentityManager (D-3a, no-op update/setLocked idempotente NO emiten), WorkspaceManager (D-4 mini, tab ops NO emiten — privacy), BookmarkManager (D-4 mini b, sidecar `bookmarks-sync-meta.json` para updatedAt sin tocar formato de bookmarks.json). **Decisiones arquitectónicas memorables**: race-safe conditional remove (engine sólo borra del queue si el slot no fue coalescido durante el upload — zero edit loss garantizado); dual-strip de tabSpecs en workspaces (push fetchRecord + apply); `'remote-applied'` event distinto de `'changed'` para evitar loop; bookmarks como single-record vs per-record (ADR §1); 8 LOC-splits ejecutados sin violations (max main.js 500). **Bug bonus**: `backup-manager.smoketest` retention test era date-flaky (asumía -50d/-52d misma semana ISO), arreglado con offsets calculados desde el lunes de la semana objetivo. **Tests acumulados: 1776 → ~2236** (+460). **Pendiente (D-3c-3c/d + D-4 polish)**: main.js wire-up (instanciar + setupSync + UI toggle + OAuth — requiere `npm start` para validar), long-poll real via `filesListFolderLongpoll` (snap 30s→<10s), tombstone GC sweep (30d retention §9), DR drill end-to-end. Detalle por sub-chunk en `docs/history/27-D3-sync-engine-resultado.md`. ADR 0026 marcado **Implemented**.

- [`2026-05-10`] [`E2-C-7`] **Extensions per-identity sharing** (1 commit, ~2h efectivas vs ~3h estimadas, cero deps nuevas, +31 tests). 2 decisiones de scope vía AskUserQuestion (recomendadas): install-en-Default + share-to-identity (sobre multi-instance Web Store full); smoke test primero (confirmó diagnóstico estático: ElectronChromeExtensions bound a defaultSession, identities custom sin extension API). **`browser/extensions-share.js`** (~265 LOC) clase ExtensionShareManager con persistence `userData/extension-sharing.json` schema `{byIdentity:{id:[extId,...]}}`. API: listInstalledInDefault (excluye WebUI), listEnabledForIdentity, reportForIdentity (matriz), enableForIdentity (load extension en partition + persist), disableForIdentity (removeExtension + persist), hookSessionInit (re-load enabled list al boot per identity). Lazy-creates `ElectronChromeExtensions` instance per partition vía `_ensureChromeExtensionsForSession()` con stub createTab/selectTab/createWindow handlers (las extensions usan storage/runtime/declarativeNetRequest mostly, no necesitan tab orchestration). Reusa `electron-chrome-extensions.fromSession()` / multi-session support. **`browser/extensions-share-handlers.js`** (~45 LOC) handler map IPC+MCP shared. **`browser/extensions-share-setup.js`** (~45 LOC, split per ADR 0005 — main.js de 511→496 post-extract). Encapsula instanciación + sessionInitHook wiring. **`browser/mcp-tools-extensions.js`** (~75 LOC) 5 MCP tools: `oz.extensions.{listInstalled, listEnabled, report, enable, disable}`. **5 IPC channels** + **preload `window.oz.extensions.*`** + **`oz.sidebar.onRequestManageExt`** back-channel. **UI**: `browser/ui/extensions-modal.js` (~210 LOC) singleton `window.OZ.ExtensionsManager`. Modal con selector dropdown de identity + tabla de Default-installed extensions. Default identity muestra "Always enabled" tag (no editable — Chrome native uninstall flow). Custom identities muestran checkbox "Enabled/Disabled" per row con auto-toggle. Empty state amigable cuando Default no tiene extensions. Auto-refresh on `oz:extensions:changed`. **Triggers**: right-click identity → "Manage extensions…", Cmd+K palette entry "Manage Extensions for Identity…" 🧩 con keywords chrome/web/store/ublock/1password/addon/plugin. **identity-context-menu.js** extendido con "Manage extensions…" entry. **31/31 tests** del nuevo `tests/extensions-share.smoketest.js` con fakes de session.extensions + Module.\_load intercept de electron-chrome-extensions: cubren listInstalled exclude WebUI, report matriz, enable/disable persistence + idempotency, Default rejection (always-enabled / use-chrome-uninstall), unknown extension rejection, missing args, persistence round-trip cross-instance, hookSessionInit re-loads enabled list para identity custom + no-op para Default. **Tests acumulados: 1745 → 1776** (+31). Lint clean. check:loc max 496 (post-extract main.js). Prettier --check clean. ADR-less. Detalle en `docs/history/26-bloque-c7-resultado.md`. Validación visual end-to-end pendiente próxima sesión (install uBlock en Default → abrir Manage extensions desde una identity custom → checkbox enable → boot tab en esa identity → confirmar uBlock activo).

- [`2026-05-10`] [`E2-C-8`] **Sidebar Compactor** (1 commit, ~1h efectivas vs ~3-4h estimadas, cero deps nuevas, cero tests nuevos — refactor UI puro). 4 decisiones de scope vía AskUserQuestion (todas las recomendadas): los 8 botones grandes (Accounts/TimeMachine/Proxies/Settings/BrowsingData/BulkOpen/Notifications/HealthCheck) → toolbar de iconos compactos en footer del sidebar; "+ New Identity" inline contextual en cada workspace row del árbol (en vez de botón grande arriba); eliminar del todo los botones grandes anteriores. **Antes**: ~50% del alto vertical del sidebar consumido por panel de modales esporádicos. **Después**: ~95% del alto para el árbol workspaces+identities (la zona del día a día), 8 íconos 32px de alto al fondo. **HTML changes** en `browser/ui/webui.html`: bloque `<div id="oz-sidebar-toolbar">` reemplaza líneas 2863-2888; cada `<button>` mantiene su ID original (oz-accounts-button, oz-tm-button, etc.) para compat con account-manager.js / time-machine.js / etc. que hacen `getElementById`. Health Check 🩺 nuevo botón (no existía botón previo) — wireado en webui.js a `window.OZ.HealthCheck.open(activeId)`. **CSS** ~140 LOC nuevos: `#oz-sidebar overflow-y:hidden` + `#oz-identity-list flex:1 + overflow-y:auto + min-height:0` (tree scroll interno, toolbar sticky bottom). `.oz-toolbar-btn` 32×32 transparent + hover bg + position:relative para badges/dots. Override `#oz-accounts-button.oz-toolbar-btn` (vault-status-dot ahora absolute top-right). `.toolbar-badge` (notif badge) absolute top-right rojo `#e85a5a` + box-shadow ring. `.toolbar-count` para tm-count/pm-count. `.ws-add-identity-btn` opacity:0 → 0.7 on row hover, 1 on hover button. **JS changes**: `sidebar.js` renderWorkspaceWrapper agrega botón "+" mini contextual por workspace row (excepto frozen/archived). Nuevo handler `handleNewIdentityInWorkspace(workspaceId)` con window.prompt(). Refactor: eliminada `handleNewIdentity` legacy + helper `_inlineRename` (~50 LOC sacados). `handleNewWorkspace` también usa prompt() ahora (consistencia). `webui.js` agrega listener al `oz-health-button` que abre HealthCheck modal con identity activa. **LOC**: sidebar.js de 522 (post-C-6) → 491 (post-extract de \_inlineRename + simplificación). Pre-commit check:loc verde (max 495). Lint clean. Prettier --check clean (incluyendo .md). **Tests acumulados sin cambios: 1745 → 1745** (refactor UI puro, sin lógica nueva — los IDs preservados garantizan que los modal openers existentes siguen funcionando). ADR-less. Detalle en `docs/history/25-bloque-c8-resultado.md`. Validación visual end-to-end pendiente próxima sesión.

- [`2026-05-10`] [`E2-C-6`] **Anti-Detect Health Dashboard** (1 commit, ~2h efectivas vs ~3h estimadas, cero deps nuevas, +43 tests). 3 decisiones de scope vía AskUserQuestion: badge inline + modal detail (sobre panel global), 4 vectores en v1 (IP↔TZ + fingerprint coherence + cookie health + proxy reachability), inline action buttons sobre solo-informativo. **`browser/anti-detect-health.js`** (~380 LOC módulo puro, sin Electron, sin fs) `evaluateHealth({identity, fingerprint, proxy, cookies, now})` retorna `{identityId, overall:red|yellow|green, vectors:{ipTimezone, fingerprintCoherence, cookieHealth, proxyReachability}}` con per-vector `{status, summary, details, fix?}`. Worst-of-N para overall, unknown ≈ green (no signal ≠ bad). **Vector 1 IP↔TZ**: usa `resolveCountry(proxy.country)` de country-locale + compara contra `fingerprint.timezone`. Same continent prefix → yellow, distinto continente → red, exact match → green. Fix kind `apply-geo-suggestion`. **Vector 2 fingerprint coherence**: cross-check interno (MacIntel ⇒ UA contiene "Mac", Win32 ⇒ no WebGL Apple/Metal, locale lang ↔ languages[0]). Red en mismatches duros UA/platform/WebGL, yellow en locale/lang soft mismatch. Fix kind `reroll-fingerprint`. **Vector 3 cookie health**: ratio expired/persistent cookies (session cookies cuentan como vigentes). >50% red, >20% yellow. Fix kind `mark-cookies-for-relogin`. **Vector 4 proxy reachability**: usa `proxy.isDisabled/lastTestedAt/failureCount` que el 1.8c daemon ya mantiene (no fires probes propios). Disabled → red, never tested / stale >24h / failureCount>0 → yellow. Fix kind `test-proxy` o `reassign-proxy`. **`browser/anti-detect-health-handlers.js`** (~220 LOC) handler map IPC+MCP shared con `get/list/applyFix`. `fetchCookiesSafe` con timeout 2s contra freezes. `applyFix` dispatch a `rerollFingerprint/applyGeoFromProxy/reassignProxy/testProxyForIdentity/markCookiesForRelogin` — reusa managers existentes (FingerprintEngine.regenerate/applyGeoSuggestion, ProxyAssignment.assignToIdentity('auto-random'), ProxyHealth.testOne, AntiLogout.flagAllForRelogin si existe + AlertManager.add fallback). **3 IPC channels** + **preload `window.oz.health.{get,list,applyFix,onChanged}`**. **3 MCP tools** `oz.health.{get,list,applyFix}` extraídos a `mcp-tools-health.js` (split per ADR 0005). **UI**: `browser/ui/health-modal.js` (~210 LOC) singleton `window.OZ.HealthCheck` con 4 vector rows (icon + name + status pill + summary + inline fix button) + header con identity color dot + overall pill + Re-check button. Patrón consistente con C-3 modal. **Sidebar dot inline**: `browser/ui/sidebar-health.js` (~85 LOC, split per ADR 0005 — sidebar.js había llegado a 528) encapsula cache + IPC listeners + render helper. Solo se renderiza el dot para yellow/red (green/unknown stay invisible). Click directo abre el modal. **Triggers triples**: right-click identity → "Health check…", Cmd+K palette entry "Anti-Detect Health Check…" 🩺 con keywords fingerprint/proxy/cookie/timezone/audit/safety, sidebar dot click. **identity-context-menu.js** extendido con "Health check…" entry. **preload `oz.sidebar.onRequestHealthCheck`** back-channel. **43/43 tests** del nuevo `tests/anti-detect-health.smoketest.js` cubriendo cada vector (unknown/green/yellow/red paths + edge cases), composición top-level (overall = worst-of, all-unknown → green, throws on missing identity.id), fix kinds presentes en results. **Tests acumulados: 1702 → 1745** (+43). Lint clean. check:loc max 495 (post-split sidebar). Prettier --check clean (incluyendo .md). ADR-less. Detalle en `docs/history/24-bloque-c6-resultado.md`. Validación visual end-to-end pendiente próxima sesión.

- [`2026-05-10`] [`E2-C validation`] **Validación visual end-to-end de C-2 + C-3 + C-5 sobre .app empaquetada PASADA** (sin commit de código — solo docs + DMG). `npm run package` + replace `/Applications/OZ Browser.app` con commit `7e34d11` + `npm run make` → `OZ Browser-0.1.0-arm64.dmg` (111MB) + `.zip` en `out/make/` (sin firmar Apple Dev, workaround `xattr -cr` aplicado). Validación vía Desktop Commander + computer-use (request_access "OZ Browser" tier full): **C-5** ✅ trigger MCP `oz.alerts.add` → badge "1" rojo en sidebar 🔔 → modal con severity dot urgent + inline action button "Open Time Machine" + auto-mark-read 800ms → persistencia `alerts.json` con `read:true` post-modal. **C-3** ✅ MCP `oz.identities.clone` con `sameFingerprint:true` → ok=true + nuevo id + fpSeed idéntico al parent → MCP `oz.fingerprint.get` parent vs clone retorna mismo UA + hwConcurrency + timezone + language (verificación end-to-end del SHA256 RNG determinístico) + UI right-click → context menu nativo orden correcto → click "Clone identity…" → modal `📑 Clone identity` con preview name colision-aware "(copy 2)" + 3 checkboxes con UA disabled correctamente cuando source no tiene UA. **C-2** ✅ pre-kill state capturado (`running.lock` + `windows.json` con bounds + workspaceId) → `pkill -9 -f "OZ Browser"` → re-launch → init() colgado en `await promptRestore()` → **dialog nativo aparece** "OZ Browser ended unexpectedly last time. Would you like to restore your previous window and tabs? [Start Fresh] [Restore]" → click Restore → log `session restored from snapshot {requested:1, created:1}` → window recreada con bounds 1280×720 + 8 tabs lazy + **bonus integration C-2↔C-5**: alert auto-emitida `"Session restored" (crash-recovery/info)` con message correcto. **Cero bugs runtime encontrados.** Los 3 features funcionan en .app empaquetada exactamente como los unit tests dijeron. Tests sin cambios (1702/1702 verde). Histories `21-bloque-c2-resultado.md` + `22-bloque-c3-resultado.md` + `23-bloque-c5-resultado.md` actualizados con sección "Validación visual ✅ PASADA".

## Histórico

- [`2026-05-10`] [`E2-C-3`] **Identity templates / Clone** (1 commit, ~1h efectivas vs ~2h estimadas, cero deps nuevas, +65 tests). 3 decisiones de scope vía AskUserQuestion: User elige inheritance (3 checkboxes con defaults: fresh fingerprint + same proxy + no UA) sobre "same EVERYTHING" (anti-detect risk) o "fresh EVERYTHING" (poco útil); solo Clone v1 (templates curados a C-XX futuro); right-click + Cmd+K palette (sin UI surface dedicada). **`browser/identity-clone.js`** (~125 LOC módulo puro testeable): `cloneIdentity({srcId, opts, identityManager, proxyAssignment?})` orquesta create + heredades. `resolveCopyName` strip-aware: clonar "X (copy)" produce "X (copy 2)" no "X (copy) (copy)". Color + workspaceId siempre heredados. Best-effort proxy assign (throw → ok=true con inherited.proxy=false). Reasons: not-found / no-identity-manager / IDENTITY_CAP_REACHED / create-failed. **`IdentityManager.create()`** extendido con `opts.fingerprintSeed` opcional (backward-compat) — habilita same-fingerprint sin tocar update() whitelist. **`identity-handlers.js`** +clone() + previewCloneName(). **2 IPC channels** + **preload bridge** + **`oz.sidebar.onRequestCloneIdentity`** back-channel. **Sidebar context menu**: "Clone identity…" después de "Edit identity…", disponible TAMBIÉN para Default y locked (clone es non-destructive — H2 lock solo bloquea remove + clearBrowsingData). **Modal `browser/ui/identity-clone.js`** (~205 LOC) singleton `window.OZ.IdentityClone`: source preview row + name input + 3 checkboxes con copy descriptiva + Cancel/Clone + Esc/Cmd+Enter shortcuts + auto-hide WebContentsView (ADR 0011). **Cmd+K palette** entry "Clone Identity…" (📑) con keywords duplicate/copy/fingerprint/sub-account/variant + modalMap. Cuando se abre desde Cmd+K sin srcId, modal usa `identities.getActive()` como fallback. **MCP**: 2 tools nuevos `oz.identities.clone` + `oz.identities.previewCloneName` extraídos a `mcp-tools-identity-clone.js` (split per ADR 0005 — mcp-tools.js había llegado a 501). **63/63 tests** del nuevo `tests/identity-clone.smoketest.js` cubriendo resolveCopyName edge cases, sameFingerprint/sameProxy/sameUA combinaciones, locked/default source clonables, cap reached, proxy save throws best-effort, integration twice. **Tests acumulados: 1584 → 1649** (+65 totales — 63 propios + 2 mcp-server contract). Lint clean. check:loc max 495 (sin cambios post-split). ADR-less. `docs/history/22-bloque-c3-resultado.md`. Validación visual end-to-end pendiente próxima sesión.

- [`2026-05-10`] [`E2-C-2`] **Crash recovery con state restore** (1 commit, ~2h efectivas vs ~3h estimadas, cero deps nuevas, +109 tests). 3 decisiones de scope preguntadas a Jose antes de codear: lockfile + clean-shutdown flag (no solo crashReporter — no cubre force-quit/OS reboot/kernel panic), workspaces + tabs lazy (no scroll/form drafts: privacy concern + RAM/disk overhead), native dialog "Restore previous session?" (no auto-restore silente, no banner). **`browser/crash-detector.js`** (~165 LOC) lockfile `userData/running.lock` con `{pid, startedAt, ozVersion}`. `init()` clasifica: clean boot / crash (PID muerto / JSON corrupto / PID inválido) / multi-instance (PID vivo distinto, NO sobrescribe). `markCleanShutdown()` borra el archivo en before-quit LATE. PID liveness via `process.kill(pid, 0)` ESRCH. Inyectables `procIsAlive`/`clock` para tests. 38/38 tests cubriendo edge cases (mkdir recursivo, idempotencia, same-PID stale). **`browser/window-snapshot.js`** (~210 LOC) persiste `userData/windows.json` schema v1 con `[{workspaceId, bounds, isMaximized, isFullScreen}]`. Daemon polling cada 2s + dedupe por JSON serializado excluyendo `capturedAt`. `flush()` sync para before-quit + `'closed'` listener. Skipea zombies (`isDestroyed()`). Por qué polling y no hooks: eventos relevantes dispersos en múltiples APIs, hookear cada uno = invasivo + frágil; polling cubre todo automáticamente. 42/42 tests cubriendo capture/read/flush/dedupe/clear/daemon lifecycle. **`browser/session-restore.js`** (~205 LOC) `promptRestore()` native dialog buttons `[Restore, Start Fresh]` con singular/plural copy. `restoreFromSnapshot()` itera entries → `browser.createWindow({workspaceId, window:bounds})` + post-create maximize/setFullScreen. **Lock 1-1 enforcement (ADR 0015)**: dedupe defensivo por workspaceId. **Workspace gone fallback**: si workspace borrado entre crash y restore → fallback al Default. **Partial restore**: try/catch por entry, partial beats none. **All-fail fallback**: garantiza ≥1 window al boot. 29/29 tests con fakeBrowser+fakeDialog. **`browser/crash-recovery-setup.js`** (~85 LOC) glue extraído de main.js per ADR 0005 (main creció a 526 LOC, post-extract bajó a 471). **Wire en main.js**: `setupCrashRecovery(this)` post-extensions pre-`createInitialWindow` + `windowSnapshot.startDaemon()` post-windows + flush + markCleanShutdown LATE en before-quit + flush en `'closed'` listener post-HX2-splice. `createWindow()` ahora hace merge defaults+`options.window` (necesario para session-restore bounds custom sin perder defaults frame/titleBar/webPreferences). **Tests acumulados: 1475 → 1584** (+109 propios del bloque). Lint clean. check:loc max 495 (sin cambios). ADR 0024 + `docs/history/21-bloque-c2-resultado.md`. Validación visual end-to-end (`kill -9` mid-session + re-launch + dialog) queda para próxima sesión.

- [`2026-05-10`] [`E2-C-4`] **Bulk multi-account opener** (1 commit, ~2h efectivas vs ~2-3h estimadas, cero deps nuevas). Resuelve use case real de Jose: "30 cuentas IG en un workspace, una tab cada una." 3 decisiones de scope: triggers triples (sidebar button + Cmd+K palette + ⌥⇧O accelerator), dos modos (`fromExisting` multi-select + `createNew` count + name pattern), target workspace dropdown elegible con default "+ New workspace…" auto-named. **`browser/bulk-opener.js`** (~270 LOC módulo puro) con `bulkOpenFromExisting`/`bulkCreateNew` que orquestan IdentityManager + WorkspaceManager + tab-handlers vía inyección. Templates `{n}` (1-indexed) y `{i}` (0-indexed). `validateInput` pre-flight (mode/count/namePattern). `resolveTargetWorkspace` rechaza archived + crea auto. Locked identities skipped con error row (ADR 0023 D5 honored). **`browser/bulk-opener-handlers.js`** con preview helpers `previewNames`/`previewUrls` cap 50 que UI invoca per-keystroke + broadcasts identities/workspaces changed post-success. **`browser/ui/bulk-opener.js`** (~390 LOC) modal segmented control + multi-select con search + select-visible + live preview rows + result panel con errors top-10. **5 IPC channels** `oz:bulkOpen:*` + preload `oz.bulkOpen.*`. **Menu.js "Go" submenu** extendido con "Bulk Open Identities… ⌥⇧O" (routing solo a focused window). **Command palette** entry "Bulk Open Identities…" con keywords mass/batch/multi/account. **40/40 tests** `tests/bulk-opener.smoketest.js` cubriendo template resolution, validateInput edge cases, resolveTargetWorkspace, both flows con FakeManagers, locked skip, missing identity, workspace move-when-different, URL pattern per-counter. Lint clean, check:loc 495/500 sin cambios. ADR-less (orquestación sobre primitivas existentes). `docs/history/20-bloque-c4-resultado.md`.

- [`2026-05-10`] [`E2-C-1`] **Cmd+K Command Palette** (1 commit, ~2h efectivas vs ~3h estimadas, cero deps nuevas). 3 decisiones de scope preguntadas a Jose antes de codear: merge de `hotfix/H1H2H3` a `main` first (12 commits fast-forward H+E2-B); scope full (identities + workspaces + tabs + 14 acciones top); fuzzy con score VS-Code-style. **`browser/command-palette.js`** (~330 LOC) módulo puro: `buildCommands(sources)` produce 4 categorías (action > tab > identity > workspace). `fuzzyMatch(q,text)` greedy subsequence con bonus `BONUS_WORD_START=12` / `BONUS_FIRST=14` / `BONUS_CONSECUTIVE=10` + penalty gap, fast-path substring `SCORE_EXACT=100`. `scoreCommand` weighting label (1.0) > keywords (0.8) > subtitle (0.6). `matchAndRank` con limit + estable tiebreaker. **`browser/command-palette-handlers.js`** `list({focusedWindowId})` bakea `activeIdentityId`/`focusedTabId` en action payloads. **`browser/ui/command-palette.js`** modal top-aligned 12vh con input + result list + footer kbd. Listener `oz.commands.onOpen` + backstop document keydown. Match highlight con `<mark>` per index. Categorías agrupadas solo en empty query (VS Code pattern). Executors: new-tab, new-identity, duplicate/lock/pin/reopen, take-snapshot, open-modal (5 modals via 2 patrones existentes), switch-identity/workspace, select-tab. Stub: move-to-new-window (no IPC todavía). **Wire:** `oz:commands:list` IPC + preload `oz.commands.{list,onOpen}` + menu.js "Go" submenu con `Command Palette… ⌘K` que envía sólo al focused window. Bonus fix: typo "Toggle Developer Tool asdf" → "Toggle Developer Tools". HTML+CSS Spotlight-style z-index 11000. **39/39 tests** `tests/command-palette.smoketest.js`. **Tests acumulados ~1285 → ~1324 (+39)**, lint clean, check:loc 495/500. Branch `feature/c1-cmd-k` desde main post-merge de `hotfix/H1H2H3`. ADR-less (sin decisiones arquitecturales nuevas — reusa patrones existentes). `docs/history/19-bloque-c1-resultado.md`.

- [`2026-05-10`] [`Etapa 3d`] **Auto-update wiring** (1 commit, ~1.5h, cero deps nuevas — `update-electron-app@^3.2.0` ya pre-instalada). `browser/auto-update.js` (~150 LOC) wrappea `updateElectronApp()` con 5 skip conditions explícitas (`!app.isPackaged`, `OZ_UPDATE_DISABLED=1`, `platform !== 'darwin'`, `!OZ_UPDATE_BASE_URL`, `!HTTPS`), logger adapter al `oz-browser.log`, try/catch interno (browser nunca crashea por updater). Defaults: `updateInterval: '1 hour'`, `notifyUser: true` (dialog nativo OS), `UpdateSourceType.StaticStorage` (numeric `1`). Wire en `Browser.init()` post-managers. **Decisión de canal:** Cloudflare R2 + StaticStorage (repo es privado, `update.electronjs.org` requiere público; R2 free tier 10GB sobra; sin lock-in S3-compatible). **Decisión de UI:** native OS dialog sobre custom topbar banner (~5 LOC vs ~80 LOC, branded queda C-XX). 14/14 tests offline (skip cases, happy path con config esperada, logger adapter forward, error handling, integration check de enum match). Lint clean. check:loc max 449/500. Setup R2 (~30 min, 7 steps) documentado en `docs/modules/auto-update.md` para que Jose lo haga post-Apple-Dev. Runtime end-to-end bloqueado por 3b/3c (sin notarización el updater falla en silencio en macOS Catalina+) — anotado en código + módulo + ADR. ADR 0021 + `docs/history/18-bloque-etapa-3d-resultado.md`. PLAN-MAESTRO actualizado con 3d ✅. README status row actualizada. **Tests acumulados: 1242 → 1256 (+14).**
- [`2026-05-10`] [`chore deps`] **Swap `stripe` → `@paypal/paypal-server-sdk@^2.3.0`** (1 commit, cero código de producto tocado, 0 vulnerabilities post-swap). `stripe@22.1.1` era pre-instalado pero nunca usado en código. Sync de docs con decisión Etapa 5 PayPal del 2026-05-10: README, OVERVIEW, DEPENDENCIES (mermaid + tokens), PROJECT-RULES-PORTABLE I7 (ahora bilingüe Stripe|PayPal con criterio de elección + nota patrón común `shell.openExternal`). Tests sin cambios (1242/1242). Lint clean. CI verde 38s (run 25627583948).
- [`2026-05-10`] [`Etapa 3a`] **Package + .dmg unsigned** (1 commit, ~30 min, cero deps nuevas, infra pre-armada de sesión previa). `npm run package` → `out/OZ Browser-darwin-arm64/OZ Browser.app` (289MB). `npm rebuild` requerido pre-primer-make: native bindings de `macos-alias` (volume.node) + `fs-xattr` (xattr.node) — deps transitivas de `appdmg` — no compilados por bug `NODE_ENV=production` skipeando devDeps. Post-rebuild: `npm run make` → `out/make/OZ Browser-0.1.0-arm64.dmg` (112MB) + `.zip` (112MB). DMG `hdiutil verify` CRC32 valid, mount con `OZ Browser.app` + `Applications` symlink. Boot del .app via `Contents/MacOS/OZ Browser` con `OZ_MCP_ENABLED=1`: 8 identities, 2 workspaces, AccountVault locked, AntiLogout 32 hosts × 8 ids, BackupManager (3 snapshots), SettingsManager v1, FingerprintEngine, BookmarkManager, ProxyManager + ProxyHealth daemon, TabDiscardDaemon, **MCP server `127.0.0.1:9223` con 97 tools** (validado vía `curl tools/list`). preload-content.js resuelve desde `app.asar` (fix del 1.5f confirmado post-package). Sin firmar: Gatekeeper bloquea, workaround `xattr -d com.apple.quarantine` o System Settings → "Open anyway" — aceptado para 3a, 3b/3c lo cierran post-Apple Dev ($99). 3b/3c/3d/3e quedan documentados con env vars + comandos en `forge.config.js` comentado y en ADR 0020. **Tests sin cambios: 1242/1242.** ADR 0020 + `docs/history/17-bloque-etapa-3a-resultado.md`. PLAN-MAESTRO actualizado con sub-bloques 3a-3e enumerados. `out/` excluido de git.
- [`2026-05-10`] [`1.10 + cierre Sub-Etapa 1A CORE`] **BLOQUE 1.10 + CIERRE SUB-ETAPA 1A CORE COMPLETO** (4 sub-fases en sesión continua, ~3-4h efectivas vs ~16h estimadas, -75% por scope ajustado, cero deps nuevas). 3 decisiones de scope: (1) sesión continua sin parar; (2) Extensions multi-identity DIFERIDA a v2 (compat risk con electron-chrome-extensions); (3) History MVP propio (Electron NO incluye history API). **1.10a Settings UI:** `settings-manager.js` schema versionado v1 con 6 secciones (general/privacy/automation/backup/onboarding/performance) + validation per-key + mergeWithDefaults forward-compat. `settings-handlers.js` handler map. `browser/ui/settings.js` modal con sidebar nav + bindings declarativos + save debounced 250ms. Botón ⚙️ Settings en sidebar. 48/48 tests. **1.10b Browsing data backend:** `download-manager.js` hookea session.on('will-download') per identity + lifecycle persistence (cap 1000). `history-manager.js` hookea tab-updated events + dedup 60s window per (identityId,url) + cap 10K + throttled save 2s + skip about:/chrome-extension:. `browsing-data-handlers.js` handler maps. UI completa diferida a 1.10.5 (backend MCP-accesible). 47/47 tests. **Refactor:** ipc-handlers.js creció a 518 LOC violando ADR 0005, split a `ipc-handlers-extra.js` con 4 funciones movidas (proxies+fingerprint+settings+browsing-data). ipc-handlers.js bajó a ~395, extra.js ~140. **1.10c Onboarding:** `browser/ui/onboarding.js` 3 pantallas (Identities → Workspaces+Vault → Proxies+Fingerprint+MCP) con next/back/skip + dot pagination. Trigger en webui.js boot via `maybeOpen()` que chequea settings.onboarding.completed. Markup + ~80 LOC CSS. **1.10d Apple Silicon perf pass:** `tab-discard-daemon.js` scan cada 5 min sobre tabs materializadas + non-pinned + non-selected + idle > N min, llama `tab.discard()` que destruye WebContentsView pero conserva tabList entry. Re-seleccionar re-materializa from pendingUrl. Toggle desde settings.performance.autoTabDiscard, no requiere restart. `Tab.discard()` + `lastSelectedAt` tracking. 26/26 tests. **Memory pressure handler + cache caps DIFERIDOS** a v1.5 (sin métricas reales para calibrar). **Tests acumulados: 1121 → 1242 (+121)** (settings-manager 48, browsing-data 47, tab-discard-daemon 26). Lint clean. check:loc max 449 (post-split). ADR 0019 + `docs/history/16-bloque-1.10-resultado.md`. PLAN-MAESTRO actualizado: 1.10 ✅, **Sub-Etapa 1A CORE ✅**. **El producto OZ Browser está functionalmente completo a nivel CORE.** Próximo: Sub-Etapa 1B (distribución, billing, Apple notarization, public launch).
- [`2026-05-10`] [`1.9 + cierre FingerprintEngine`] **BLOQUE 1.9 FINGERPRINTENGINE "GHOST+" CERRADO COMPLETO** (5 sub-fases en una sesión, ~5h efectivas vs ~10h estimadas, cero deps nuevas). 3 decisiones de scope preguntadas a Jose: (1) 11 vectores (top-9 + canvas + WebGL); excluidos AudioContext/WebRTC/fonts; (2) GeoIP coherence vía tabla pura cubre 80%, no fetch real; (3) Pixelscan/CreepJS CI suite diferida a sub-bloque 1.9.5. **1.9a:** `fingerprint-engine.js` (~390 LOC) clase con SHA256-stream RNG determinístico (mismo seed = mismo perfil siempre, distintos seeds = perfiles coherentemente variados), 5 blueprints reales (mac-arm64-chrome, mac-x64-chrome, win-x64-chrome, win-x64-edge, linux-x64-chrome) con UAs Chrome 135 + WebGL renderers compatibles per blueprint, 11 LOCALE_PROFILES + speech voices per locale + 5 plugins PDF estándar. API: getOrCreate, regenerate, applyGeoSuggestion (preserva UA/screen, muta solo locale fields). Persistencia fingerprints.json (NO regenera per session). 96/96 tests. **1.9b+1.9c (combinado):** `preload-fingerprint.js` (~290 LOC) content script per-identity que aplica overrides en page world via `webFrame.executeJavaScript` ANTES del primer JS. Pattern: `ipcRenderer.sendSync('oz:fingerprint:request')` (local <1ms) + buildOverridesScript IIFE con todos los hooks: navigator.{userAgent, appVersion, appName, vendor, platform, hardwareConcurrency, deviceMemory, language, languages, plugins, mimeTypes, getBattery}, screen.\* + dpr, Intl.DateTimeFormat + Date.getTimezoneOffset, speechSynthesis.getVoices, canvas (toDataURL/toBlob/getImageData con noise determinístico via mulberry32), WebGL getParameter (parámetros 7936/7937/37445/37446). Refactor identity-manager: `addSessionInitHook(fn)` permite múltiples hooks ordenados (1.8b proxy + 1.9b FP). Two-layer override: setUserAgent (network) + preload (page world). **1.9d:** GeoIP coherence sin deps. `country-locale.js` (~80 LOC) tabla 47 países → {timezone, languages, locale}. `fingerprint-handlers.js` con applyGeoSuggestion (acepta {country:'JP'} resolved via tabla, o {timezone, languages, locale} verbatim). Refactor proxy-handlers: assignToIdentity/Workspace return type `{ok, geoSuggestion?}` para surfacear suggestion en UI/MCP. 39/39 tests. **1.9e:** `mcp-tools-fingerprint.js` con 6 tools nuevos. mcp-server: 127 → 132. **1070/1070 tests verde** (+140 vs 930). Lint clean. ADR 0018 + `docs/history/15-bloque-1.9-resultado.md`. Decisiones honestas excluidas v1: AudioContext (perf), WebRTC disable (rompe video calls), fonts subset (rompe Google Docs). PLAN-MAESTRO actualizado (1.9 ✅).
- [`2026-05-10`] [`1.8 + cierre Proxy Manager`] **BLOQUE 1.8 PROXY MANAGER CERRADO COMPLETO** (5 sub-fases en una sesión, ~6h efectivas vs ~12h estimadas, cero deps nuevas — csv-parse ya pre-instalada). 3 decisiones de scope preguntadas a Jose antes de codear: (1) solo Oxylabs real, BrightData/Smartproxy/IPRoyal stubs marcados "Coming soon"; (2) bandwidth meter placeholder en 1.8 (real en 1.10); (3) health daemon liviano (cada 30 min, auto-disable después de 3 fails). 1.8a backend: `proxy-manager.js` clase ProxyManager con CRUD + persistencia proxies.json + autoAssign random/round-robin + health helpers + bulkAdd. Modelo Proxy 12 fields, isActive (user) vs isDisabled (daemon) separados. 9 IPC channels base + 58/58 tests. 1.8b assignment: `proxy-assignment.js` con jerarquía Identity > Workspace > defaultStrategy, proxy-assignments.json storage, value puede ser proxyId | 'auto-random' | 'auto-round-robin' | null. Per-tab proxy NO supported v1 — limitación honesta documentada en ADR 0017 (session.setProxy es per-session; workaround: Duplicate→New Identity). main.js wirea identity.setProxyResolutionHook + app.on('login') handler para HTTPS auth (ADR 0004). 5 IPC channels + 30/30 tests. 1.8c health: `proxy-health.js` con testOne (TCP+CONNECT handshake parse, status 200/407 → ok), testAll paralelo, daemon setInterval cada 30 min, auto-disable después de 3 fails seguidas + Notification del OS. SOCKS5 solo TCP reachability. Hooks tcpConnect/connectViaProxy inyectables para tests sin red real. 2 IPC channels + 25/25 tests. 1.8d CSV+UI: `proxy-csv.js` con csv-parse/sync (formato Ghost-compat: protocol,host,port,username,password,tags,country,name; tolerante a reorder + alias user/pass). `proxy-providers.js` con Oxylabs real (`expandOxylabs` con sessid sequential pattern `customer-{user}-cc-{country}-sessid-NNNNNN-sesstime-{min}`) + 3 stubs COMING_SOON. UI dedicada `browser/ui/proxy-manager.js` (~410 LOC) modal full-screen con 3 vistas (list/editor/providers), tabla con status badges + Test/Edit/Disable/Delete por row, toolbar con Add/Import/Export/Providers/Test all. Botón "🌐 Proxies" en sidebar (azul). Markup HTML + ~150 LOC CSS en webui.html. 8 IPC channels + 45/45 tests. 1.8e MCP+cierre: `mcp-tools-proxies.js` con 22 tools nuevos. Contract test extendido (regex + exempts proxies:pickCsv\*). mcp-server: 105 → 127. **908/908 totales verde** (+158 vs 750 al cierre 1.7). Lint clean. check:loc max 449. ADR 0017 + `docs/history/14-bloque-1.8-resultado.md`. PLAN-MAESTRO actualizado (1.8 ✅).
- [`2026-05-10`] [`1.7 + cierre Tab Context Menu`] **BLOQUE 1.7 TAB CONTEXT MENU CERRADO COMPLETO** (4 sub-fases en una sesión, ~5h efectivas vs ~6h estimadas, cero deps nuevas). 1.7a backend tab-actions: `browser/tab-context-handlers.js` (handler map para reload, duplicate, duplicateInTemporary, duplicateInIdentity, duplicateInNewIdentity, refreshAllInIdentity, moveToNewWindow auto-creates "Window N" workspace por ADR 0015 lock, pin/unpin, mute/unmute, closeOthers/closeToRight con preservación de pinned). `browser/tab-context-menu.js` (template builder para Menu.buildFromTemplate — 16 opciones Ghost-style con submenus dinámicos para "Create New Tab in Identity / Move to Workspace / Duplicate into Identity"). 13 IPC channels nuevos `oz:tabs:*` + `oz:tabs:contextMenu(tabId)` que abre menú nativo via Menu.popup() en main process. 64/64 tests del nuevo `tab-context-handlers.smoketest.js`. 1.7b bookmarks MVP + clear browsing data: `browser/bookmark-manager.js` (CRUD + persistencia bookmarks.json + dedup por (identityId,url) + removeByIdentity bulk para Bloque 1.10 cleanup) + `browser/bookmark-handlers.js` (handler map). `identity-handlers.clearBrowsingData(identityId, scope)` con scope ∈ {cookies, storage, both} usando session.clearStorageData + clearCache. 5 IPC channels bookmarks + 1 channel clear. 50/50 tests del nuevo `bookmark-manager.smoketest.js`. 1.7c cookies I/O 4 formatos: `browser/cookies-io.js` (~290 LOC, módulo puro encode/decode sin Electron — testeable sync) con OZ JSON nativo (round-trip lossless), Netscape cookies.txt (estándar curl/wget con #HttpOnly* prefix tolerance), AdsPower JSON (devtools-style con storeId="0"), Multilogin JSON (devtools-style sin storeId). `browser/cookies-handlers.js` (~170 LOC) bridge a session.cookies.{get,set} + native file dialogs `pickExportPath`/`pickImportPath` con extensión por formato. Bug atrapado por test: el check `line.startsWith('#')` en decodeNetscape skipeaba `#HttpOnly*`prefix antes de detectarlo — fix: detectar #HttpOnly_ FIRST, luego comment. 72/72 tests del nuevo`cookies-io.smoketest.js`. 1.7d UI + atajos + MCP + cierre: sidebar.js + tabstrip.js right-click ahora invoca el menú nativo via `oz:tabs:contextMenu`(replaces el HTML ctx-menu del 1.4d — diff -88/+12 LOC). Atajos en menu.js: Cmd+T (new tab current id), Alt+G (default), Alt+N (new identity), Alt+D (duplicate), Alt+S (move to new window), Alt+P (pin/unpin), Cmd+Shift+J (devtools). 23 MCP tools nuevos en`browser/mcp-tools-tab-context.js`(split por ADR 0005 — mcp-tools.js sigue bajo 500 LOC). Contract test IPC↔MCP extendido para bookmarks + cookies (atrapó missing oz.bookmarks.get + el oz:tabs:contextMenu como UI-only exempt). **750/750 tests verde** (+15 mcp-server por nuevos tools). Lint clean. check:loc max 447. Detalle en`docs/history/13-bloque-1.7-resultado.md`. (ADR 0016)
- [`2026-05-10`] [`1.6 + cierre Time Machine`] **BLOQUE 1.6 TIME MACHINE CERRADO COMPLETO** (3 sub-fases, ~4h efectivas, cero deps nuevas). `browser/backup-manager.js` (452 LOC) con clase BackupManager + formato custom .ozbackup `[u32 headerLen][headerJson][iv][authTag][AES-GCM(gzip(flatpack(payload)))]`. FlatPack format propio sin tar (cero deps). Reusa master key del Vault (`Vault.getMasterKey()` getter público nuevo). Walk recursivo de userData/Partitions/_ — backup completo bit-perfect (configs + vault.enc + cookies/IndexedDB/localStorage). `browser/backup-handlers.js` (165 LOC) con vault gate uniforme + auto pre-restore safety net (restore SIEMPRE crea pre-restore snapshot antes; aborta si falla). 5 triggers: manual (Cmd+Shift+B + UI), pre-quit (before-quit hook), pre-overwrite-total (hook en excel-handlers OVERWRITE_TOTAL), pre-restore (auto), daily-3am (setInterval cada hora con duplicate-detection). Retention: keep all daily 30d + 1 per ISO week forever para older. 5 IPC channels + 5 MCP tools `oz.timemachine._`. `browser/ui/time-machine.js`(310 LOC) con modal 2-vistas (locked/list), botón ⏱ Time Machine en sidebar con count realtime, restore con confirm + alert "restart OZ" post-success. **40/40 tests** del nuevo`backup-manager.smoketest.js`cubriendo flatpack round-trip, AES-GCM tamper detection, restore bit-perfect (sha256 hash), retention policy, isoWeek UTC-safe (bug arreglado: getFullYear local TZ → getUTC*). **549/549 totales verde**. Lint clean. check:loc max 452. **Validación visual via Desktop Commander**: 46 MCP tools, BackupManager loaded en boot, vault unlock OK, 2 snapshots creados manualmente con 196 files cada uno, 29KB compressed, files en disco bajo`data/snapshots/`. **C-19 anotado en memoria**: cloud backup vía Dropbox como servicio premium futuro (post-Etapa 7-OFFICE) — la base local ya queda compatible (file-standalone + cifrado client-side = zero-knowledge backend). Detalle en `docs/history/12-bloque-1.6-resultado.md`.
- [`2026-05-10`] [`1.5f + cierre 1.5 ⭐`] **BLOQUE 1.5 CORE CERRADO COMPLETO.** `browser/ui/account-manager.js` (~440 LOC) + `browser/ui/account-manager-render.js` (~125 LOC, split por ADR 0005) — modal full-screen con 4 vistas conmutables (lock / list / editor / import). Botón `🔐 Accounts` arriba del sidebar con dot que refleja vault status (verde unlocked, amarillo locked). List view con search por site/username + filtros identity/workspace/status, chips de identity y workspace con dot color, badge de status, edit/delete por row. Editor form 7 fields. Import view con picker de los 4 modos + double-confirm para OVERWRITE_TOTAL. **Native auto-save dialog**: el handler IPC `oz:accounts:proposeAutoSave` ahora muestra `dialog.showMessageBox` nativo del OS encima del browser cuando 1.5c detecta login submit; si user confirma, persiste vía `accounts.create/update` directamente. **Native file dialogs** para Export/Import (`oz.excel.pickExportPath` / `pickImportPath` IPC wrappers desde main, marcados UI-only en contract test exempt list). **Identity cap remove (1.5f-D):** Jose maneja 50+ accounts, default flippeado de free-por-defecto a paid-por-defecto (`OZ_TIER=free` ahora opt-in). Tests ajustados. **504/504 totales verde**, lint clean, check:loc max 443. Cero deps nuevas. **Bloque 1.5 CORE entero cerrado en 6 sub-fases (1.5a→1.5f), 504 tests acumulados, ~9-10h efectivas vs ~12-14h estimadas.** El producto vault es operacional end-to-end: Keychain auto-gen + AES-256-GCM + auto-fill silent + auto-save nativo + anti-logout cookies + Excel I/O 4 modos + Account Manager UI completo. Detalle en `docs/history/11-bloque-1.5-resultado.md`.
- [`2026-05-09`] [`1.5e`] **Excel I/O + bulk identity creation**. `browser/excel-io.js` (200 LOC) con exceljs (libre de CVEs vs xlsx community). Export a `.xlsx` con 11 columnas (Workspace, Identity, Site, Username, Password, 2FA Secret, Last Login, Status, Cookies Count, Last IP, Notes), header styling. Import con detección de columnas por header (tolera reordenamiento). `browser/excel-handlers.js` (200 LOC): handler `exportToFile` + `importFromFile(filePath, mode)` con 4 modos: PERMANENT_MERGE (update por (identityId,site,username) + add rest), EPHEMERAL_SESSION (parse only no persist), NEW_WORKSPACE (workspace dedicado para todo el batch), OVERWRITE_TOTAL (REEMPLAZA todo el vault — caller debe Time Machine antes). Bulk identity/workspace creation: nombres faltantes auto-creados via `findOrCreate*`. 2 IPC channels + preload bridge + 2 MCP tools. Contract test extendido para incluir `excel`. Round-trip lossless validado: site/username/password/totpSecret/notes/status/lastLoginAt timestamp ms preservados. **25/25 tests** del nuevo `excel-io.smoketest.js` con archivos .xlsx temporal. **503/503 totales verde**. Lint clean. Cero deps nuevas (exceljs ya estaba pre-instalado).
- [`2026-05-09`] [`1.5d`] **Anti-logout — extender session cookies de redes sociales + detección de logout** (DIFERENCIADOR vs Ghost). `browser/anti-logout.js` (230 LOC). Hook `cookies.on('changed')` por cada identity session — si una cookie de un host whitelisted (32 entries del set, derivado de site-templates) llega con `session=true`, la re-set con `expirationDate = now + 365d`. Loop guard via Map (cooldown 1h por cookie). Detección de logout: cookie social removida con `cause='explicit'/'overwrite'` + vault unlocked + matching account → flag `status='needs_relogin'` + system notification. Auto-relogin queda para 1.5c (cuando user navega manual al /login). Health check daemon difericdo a futuro (cookie absence detection cubre 80% sin overhead). Wireado en `main.js` post-Vault. `identity-handlers.create` hookea identities nuevas. **38/38 tests** con FakeSession + FakeIdentityManager + FakeVault + FakeNotification. **476/476 totales verde**. Lint clean. check:loc max 441. Cero deps nuevas.
- [`2026-05-09`] [`1.5c`] **Site templates + Auto-fill + Auto-save**. `browser/site-templates.js` (220 LOC, pure data + funciones puras) con 10 plataformas v1: X/Twitter, Instagram, Facebook, TikTok, LinkedIn, Google, Reddit, Threads, Telegram, Discord. Selectores CSS + login URL regex patterns + flow type (one-step/two-step/phone-only) + loggedInIndicator (anti-logout 1.5d). API: matchByHost, matchByLoginUrl, isLoginUrl, siteIdForUrl. `browser/preload-content.js` (190 LOC) content script per-identity wireado via session.setPreloads en identity-manager.getSession(). Auto-fill silent + SPA detection (interval 1.5s checking location.href change) + waitForSelector con MutationObserver + setInputValue React-compatible (native setter + dispatch input/change). Auto-save hook submit con capture phase. account-handlers.js: 2 nuevos handlers `getCredentialsForSite(site, identityId)` y `proposeAutoSave({site, username, password})` — vault-gated. **Security key**: el preload NO sabe ni necesita saber identityId; el main IPC handler lo resuelve via `IdentityManager.identityIdForSession(event.sender.session)` — un renderer comprometido NO puede impersonar otra identity. 2 IPC channels nuevos + preload bridge + 2 MCP tools en mcp-tools-vault.js. ESLint config extendido con browser globals (MutationObserver, Event, etc.) para preload-content.js. **125/125 tests** del nuevo site-templates.smoketest.js + 51 handlers + 30 vault + 28 identity + 83 mcp + 29 move + 36 window-ws + 56 ws-manager = **438/438 totales verde**. Lint clean. Cero deps nuevas.
- [`2026-05-09`] [`1.5b`] **Account CRUD + handlers + IPC + MCP**. `browser/account-handlers.js` (220 LOC) con dos handler maps puros (vault + accounts), mismo patrón que identity/workspace handlers. Vault gate: todas las operaciones accounts._ devuelven `{__error:{code:'LOCKED'}}` si vault no está unlocked (no throw — error estructurado uniforme IPC↔MCP). Modelo Account final con 13 campos. 10 IPC channels (`oz:vault:_`+`oz:accounts:_`). Preload bridge `window.oz.vault._`+`window.oz.accounts.\*`. 10 tools MCP nuevos en `mcp-tools-vault.js`(split de mcp-tools.js para mantener bajo 500 LOC ADR 0005). Contract test IPC↔MCP extendido para vault + accounts. Vault NO se auto-unlock al boot (UX choice — Keychain prompt solo cuando user explícitamente abre Account Manager, no en cada cold start). lock() en before-quit para wipe de master key.`vault.status()` devuelve accountsCount=null si locked (anti metadata leak). 51/51 tests handler + 81/81 mcp (con 10 nuevos del catalog) = **311/311 totales verde**. Lint clean. check:loc verde (max 441). Cero deps nuevas.
- [`2026-05-09`] [`1.5a`] **Account Vault crypto + Keychain auto-gen** (CORE primera sub-fase). `browser/account-vault.js` (290 LOC): clase `Vault` con AES-256-GCM, master key 32 bytes random auto-generada al primer uso y guardada en macOS Keychain via `@napi-rs/keyring`. Modo simplificado vs ADR original — sin scrypt KDF porque la key tiene 256 bits de entropía nativa (no derivada de password humano), no hay vector de brute-force offline. Header del vault: `{version:1, mode:'auto', cipher:{algo,iv,authTag}, ciphertext}`. API: `unlock()/lock()/getAccounts()/setAccounts()/destroy()`. VaultError con codes (LOCKED/VAULT_TAMPERED/VAULT_VERSION_MISMATCH/etc). Keychain port inyectable para tests. **30/30 tests verde** cubriendo first-time setup, round-trip persist, idempotent unlock, lock wipes state, copia profunda en getAccounts, IV cambia per save (anti-nonce-reuse), destroy borra file+key, detect tampering (flip 1 byte → VAULT_TAMPERED), version mismatch, crypto primitives \_encrypt/\_decrypt. Total proyecto: 250/250. Lint clean. ADR 0008 actualizado con la decisión de auto-gen vs passphrase.
- [`2026-05-09`] [`chore: electron upgrade`] **Electron 37.10.3 → 42.0.1** (+ electron-chrome-extensions 4.6.0→4.9.0, electron-chrome-web-store 0.10.0→0.13.0). Mini-bloque ~30 min: backup package.json/lock → npm install latest → manual `node node_modules/electron/install.js` (postinstall hook no auto-corrió en npm 11) → 220/220 tests verde post-upgrade → smoke visual con OZ corriendo + screenshot confirmando UI idéntica + persistencia de tabSpecs hidratada correctamente desde disco previa. MCP server arrancó con 27 tools, runtime metric memoria 166 MB (comparable con 37). Cero cambios de código requeridos — APIs compat full. Commit aislado.
- [`2026-05-09`] [`1.4e + cierre 1.4-WS`] **Bloque 1.4-WS Workspace Manager cerrado completo.** MCP tools `oz.workspaces.*` (13 tools agregados a mcp-tools.js: list, listActive, get, getActive, setActive, create, update, duplicate, archive, restore, freeze, unfreeze, remove). Contract test IPC↔MCP extendido para incluir dominio workspaces, exempts agregados (rename/setColor wrappers de update). Quick Tabs 4 modos implementados en `window-workspace.hydrateWorkspace`: on-click (default lazy), load-all (materializa todas con confirmación si N>10), one-by-one (stagger 250ms), on-click-confirm (UX en sidebar). UI: submenu "Quick Tabs: <mode> ▸" en context menu de cada workspace pill. **220/220 tests verde** (28 identity + 71 mcp + 29 move + 36 window-ws + 56 ws-manager). Lint clean. Cero deps nuevas. Bloque cerrado en 5 commits: 1.4a backend → 1.4b switch+lock → 1.4c sidebar UI → 1.4d drag-drop → 1.4e MCP+Quick Tabs.
- [`2026-05-09`] [`1.4d`] Drag-drop tabs entre workspaces + right-click "Move to workspace…": `tab-handlers.moveToWorkspace(tabId, targetWorkspaceId)` con strategy snapshot → appendTabSpec(target) → destroy en source. Si target activo en otra ventana, recrea live ahí. Edge cases: target-not-found, target-archived (rechazado), tab-not-found, mismo WS = noop, frozen permitido (runtime). IPC channel `oz:tabs:moveToWorkspace` + preload bridge `window.oz.tabs.moveToWorkspace`. UI: `.oz-tab` draggable HTML5 con visual `.dragging` opacity, `.workspace-pill` droppable con visual `.drop-target` (border accent + shadow + scale), right-click sidebar tab → ctx menu con submenu dinámico "Move to workspace… (N)" filtrando WS no-actuales/no-archivados. Tool MCP `oz.tabs.moveToWorkspace` agregado a mcp-tools.js (atrapado por contract test IPC↔MCP del 1.3-MCP). 29/29 tests del nuevo `move-to-workspace.smoketest.js` + suite total 207/207 verde. Lint clean.
- [`2026-05-09`] [`1.4c`] Workspace switcher UI: pills horizontales arriba del sidebar, CRUD inline (rename via dblclick o ctx menu, duplicate, archive/restore con toggle "Show archived (N)" oculto si vacío, freeze/unfreeze con 🔒 visual + name muted, delete con confirm). `+ New Workspace` inline editor (mismo patrón que `+ New Identity`). Lock UX: alert "already open in another window" cuando setActive rechaza. `preload.js` expone `window.oz.workspaces.*` (15 métodos + onChanged + onActiveChanged). `browser/ui/workspace-switcher.js` IIFE con `OZ.WorkspaceSwitcher` class. CSS pills + ctx-menu reusado. Boot wireado en `webui.js`. 177/177 tests verde, lint clean.
- [`2026-05-09`] [`1.4b`] Workspace switch logic + ventana 1-1 lock exclusivo: `browser/window-workspace.js` (módulo puro testeable, ~210 LOC) con `switchWorkspace` (snapshot → destroy WebContentsViews → hydrate lazy desde tabSpecs → select activeTabId), `hydrateWorkspace`, `snapshotWindowToWorkspace` (sync flush para no perder en crash), `releaseOnDestroy`, `findWindowOwning`. `TabbedBrowserWindow` extendido con `workspaceId` + `switchToWorkspace()` + snapshot+release on `close`/`destroy`. `WorkspaceManager` activado con throttled save 2s. `Tab.toSpec()` + `Tabs.toSpecs()` + `tab.pinned` propagado. 36/36 tests con FakeTabs mock. **177/177 totales verde.** Cero deps nuevas.
- [`2026-05-09`] [`1.4a`] Workspace Manager backend: `workspace-manager.js` (CRUD + persistencia + freeze/archive/duplicate + tabSpecs management + throttled save), `workspace-handlers.js` (handler map IPC↔MCP), 15 IPC channels `oz:workspaces:*`, smoke test 56/56. Default WS auto-creado ("General Browsing"). Cero deps nuevas. (ADR 0015 — workspace model + ventana 1-1 lock exclusivo + lazy tabSpecs).
- [`2026-05-09`] [`1.3.5-CI + 1.3.6-DX`] GitHub Actions (lint + check:loc + smoke tests en macos-latest, cron nightly, status badge). ESLint v9 flat config + Prettier + Husky pre-commit + lint-staged. Format pass sobre 77 archivos. Bug fixes menores (no-prototype-builtins, unused imports, catch \_e). 85/85 tests verde post-format. Cero deps de prod nuevas. (ADRs 0013, 0014).
- [`2026-05-09`] [`1.3-MCP`] OZ MCP server (HTTP localhost :9223 + SSE + stdio bridge). 13 tools v1 (identities CRUD + tabs + system.getMetrics + events.subscribe). Hand-rolled JSON-RPC, cero deps nuevas. Refactor identity-handlers/tab-handlers como maps puros consumidos por IPC y MCP. 57/57 smoke + contract test IPC↔MCP. Pasada estructural completa (PLAN v5, scripts/check-loc.js, BENCHMARKS, CHANGELOG, CHECKLIST). ADR 0008 actualizado con audit de deps + Etapa 3 corregida (Forge → update-electron-app). (ADR 0012).
- [`2026-05-09`] [`1.2`] Identity Manager + lazy tabs + sidebar (n) count + custom UA per-identity (ADR 0010) + free-tier cap 3 + tab-create logging + smoke test 28/28 + bugs detectados en visual test (IIFE wrap por classic scripts compartiendo lexical scope, modal cubierto por WebContentsView nativa → ADR 0011).
- [`2026-05-09`] [`1.1`] Foundation: fork de electron-browser-shell, repo privado en GitHub, tabs+omnibox+back/forward base, layout sidebar+content.
- [`2026-05-09`] [`Etapa 0`] Validación técnica spike: Electron + partition isolation + proxy auth con Oxylabs HTTPS confirmado funcional.

---

## Reglas para mantener este archivo

- **Cierre de bloque = un commit con el cambio aquí.** Sin esa línea el bloque no está cerrado (regla del CHECKLIST-CIERRE-BLOQUE).
- La línea va arriba del histórico (más reciente primero).
- Si un bloque introduce una breaking change pública (afecta CLI args, env vars, config files, MCP tools shapes), **prefijar con `[BREAKING]`**.
- Si un bloque introduce un ADR nuevo, mencionarlo entre paréntesis (e.g., "ADR 0012").
- No copiar el detalle del history acá. Esto es 1-liner; el doc de history es el cierre completo.

## Convenciones de etiquetas

- `1.2`, `1.3-MCP`, `1.4-WS`, etc. — Bloque del Plan Maestro.
- `Etapa N` — para etapas (Etapa 0 spike, Etapa 3 distribución, etc.).
- `chore` — limpieza interna sin cambio de feature (e.g., format pass del 1.3.6-DX).
- `[BREAKING]` — incompatible con versiones anteriores.
